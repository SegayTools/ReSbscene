# sbscene / svo 未知项解析计划

## 目标

继续把已知未知项拆成可验证的结构字段。每推进一项都需要满足：

- 解析器能保留原始值，不丢未知字段。
- Ras 样本能完整解析到 EOF。
- CLI / manifest / docs 能显示确认结论。
- 无法确认的字段继续标记为候选或 unknown，不强行命名。

## 当前完成判定

**状态：完成（当前本地 `surfboard` / `surfboard_EN` 静态样本范围）。**

截至本轮，现有本地 `surfboard` / `surfboard_EN` 样本中可通过静态解析验证的结构项已同步到 survey JSON、文档和本计划。2026-05-29 复核现有 JP/EN full survey 后，scene 314/314 与 318/318 全解析、warning 为 0、unknown VTBF type code 为空；SVO 47/47 与 47/47 全解析，YABX unparsed bytes 为 0。VTBF 26 类 tag、158 个 `tag|field id|type` 字段目录组合已作为正式 raw 结构数据固化；JP/EN 的字段目录 key 集合完全一致。剩余条目属于运行时语义、跨版本稳定性、hash/checksum 规则或写回设计问题；在没有新版本样本、运行时代码或 writer roundtrip 证据前，继续保留为 `unknown` / `candidate`，不作为正式数据。

## 完成口径

本计划的“完成”只表示当前本地样本和解析器能静态验证的结构/内容数据已落地，不表示所有运行时语义都已命名。正式数据必须能从 `out/survey-surfboard-all.json`、`out/survey-surfboard-en-all.json`、解析器模型或原始样本重新计算出来；候选命名只用于导航和排查，不作为协议事实。

| 范围 | 完成状态 | 证据边界 |
| --- | --- | --- |
| `.sbscene` VTBF 块结构 | 完成当前样本静态解析 | 26 类 tag 全部无 trailing bytes；未知 type code 为空；字段目录 158 类，JP/EN key 集一致。 |
| `.sbscene` 内容表 | 完成 raw 结构保留 | NODE/TRS2/NCAT/资源块/动画块均保留原始字段和值；运行时 bit、颜色混合、引用选择仍不命名。 |
| `.svo` AVTS/YABX | 完成当前样本静态解析 | 47+47 个 SVO 全解析；YABX object payload unparsed bytes 为 0；header unknown words 仅分类排查。 |
| 文档与计划 | 完成当前样本结论同步 | `docs/*.md` 与 survey JSON 使用 confirmed raw / candidate / unknown 边界；未证实结论不写成正式数据。 |

VTBF 字段目录按 tag 的完成快照如下。`Field/type combinations` 是 `tag|field id|type` 的 distinct 数量；`ParamHigh != field-count` 只记录 raw 结构差异，不解释语义。

| Tag | Field/type combinations | JP blocks | EN blocks | JP ParamHigh != field-count | EN ParamHigh != field-count |
| --- | ---: | ---: | ---: | --- | --- |
| `ANIM` | 4 | 1579 | 1614 | - | - |
| `CAM ` | 6 | 314 | 318 | - | - |
| `CAST` | 0 | 314 | 322 | - | - |
| `CATR` | 6 | 538 | 571 | - | - |
| `CIMG` | 8 | 7063 | 7228 | - | - |
| `CNUM` | 27 | 217 | 217 | 26->27 x217 | 26->27 x217 |
| `CREF` | 1 | 7558 | 7697 | - | - |
| `CRFD` | 7 | 614 | 622 | - | - |
| `CROP` | 1 | 2906 | 2922 | - | - |
| `CSLI` | 13 | 26 | 26 | - | - |
| `DATA` | 0 | 314 | 322 | - | - |
| `KEY ` | 8 | 94611 | 95558 | - | - |
| `LAYR` | 4 | 314 | 322 | - | - |
| `MOT ` | 2 | 32428 | 32742 | - | - |
| `NCAT` | 10 | 314 | 322 | - | - |
| `NODE` | 9 | 314 | 322 | - | - |
| `PROJ` | 5 | 314 | 318 | - | - |
| `SCN ` | 6 | 314 | 318 | - | - |
| `SLIC` | 9 | 26 | 26 | - | - |
| `SRCK` | 0 | 314 | 318 | - | - |
| `SRFF` | 0 | 314 | 318 | 19456->0 x314 | 19456->0 x318 |
| `TEX ` | 5 | 2906 | 2922 | - | - |
| `TEXL` | 2 | 308 | 312 | - | - |
| `TEXT` | 8 | 105 | 131 | 7->8 x105 | 7->8 x131 |
| `TRK ` | 5 | 94611 | 95558 | - | - |
| `TRS2` | 12 | 314 | 322 | - | - |

## sbscene 未知项

| 区域 | 未知项 | 当前状态 | 后续解析动作 |
| --- | --- | --- | --- |
| VTBF header | `RootParam` | surfboard/surfboard_EN full survey 中 314/314 与 318/318 个场景根块均为 raw `0x0100004C`，拆分为 little-endian `ParamLow=1 / ParamHigh=19456`；用途仍未知。解析器现已在 `VtbfBlock.ParamRawHex` 中保留原始 4 字节。 | 继续收集更多版本/目录样本；比较 raw hex 与 low/high 拆分后的语义。 |
| `vtc0` | `ParamLow/ParamHigh` 通用语义 | `KEY.ParamHigh / 5` 已确认 key 数。已新增 full-survey 结构矩阵：`VtbfTagCounts`、`VtbfTagParamRawCounts`、`VtbfTagParamLowHighCounts`、`VtbfTagParamHighPropertyCountCounts`、`VtbfTagTrailingByteCounts`、`VtbfKeyParamHighModulo5Counts` 和 `VtbfFieldDirectory*`。重新生成 JP/EN full survey 后，314/318 个场景均解析成功，observed tag 为 26 类，字段目录为 158 类，JP/EN 的 `VtbfFieldDirectoryCounts`、`VtbfFieldDirectoryBlockCounts`、`VtbfFieldCountValueCounts`、`VtbfFieldStrideValueCounts` key 集合完全一致；未知 type code 为空，所有块 trailing bytes 为 0；`KEY.ParamHigh % 5` 在 JP/EN 的 94,611/95,558 个 KEY 块上均为 0，`KEY.ParamHigh` 与实际字段数无 mismatch；`TRK` 均为 `ParamLow=1 / ParamHigh=5 / Fields=5`。`MOT.ParamLow` 与 `MOT.0x52` 在 JP/EN 的 32,428/32,742 个 motion 中均等于实际 track 数；`ANIM.ParamLow` 有 412/444 个动画比实际 motion 数多 1，`ANIM.0x50` 有 1 个 `+2` 异常。新增 `AnimationField50MotionOrMaxTrackRelationCounts` 后，唯一异常在 JP/EN 均为 `equalsMaxMotionTrackCountOnly`，但 full survey 同时显示 `0x50` 与最大 motion track 数也不是全局规则，因此 ANIM count 字段不能写成全局硬规则。 | 已完成当前样本的字段目录固化；后续只在新增样本、运行时代码或 writer roundtrip 提供证据时提升字段语义。`ParamLow/ParamHigh` 不写成全局统一语义。 |
| `NODE` | flags bit | 已输出 exact flags 与 bit 级表，并为 JSON/survey 模型新增 `flagBits`/`NodeFlagBitCounts` 和 `NodeFlagBit*` 节点级交叉聚合。full survey 中 exact flags 为 28 类，已在格式文档列出 JP/EN 分布；observed bits 为 `0/1/2/8/9/10/11/15/16`，日文计数为 7677/640/217/10706/11551/10451/11842/325/20，EN 为 7850/648/217/10928/11781/10644/12073/335/26。新增交叉统计显示 bit0 的 CIMG target node 为 7063/7214，bit11 为 7061/7214，bit16 为 20/26；bit11 不是全局必有，`0x701` 两个节点与 `0x700` 一个节点不带 bit11。 | 收集更多版本样本或运行时代码确认 bit 级精确语义，尤其是 bit 1/2/8/9/10 的分工、bit 16 特例，以及 `0x700/0x701` UI 节点的用途。 |
| `TRS2` | 材质色、illumination、vertex color、multi position/size | 已输出字段出现次数和值分布；颜色字段当前按 `A,R,G,B` 候选解释。已新增颜色/alpha track full survey 交叉统计：JP/EN 分别有 5,726/5,774 条颜色 track、11,157/11,231 个 key，所有 key 都在 `0..1`，所有目标节点都有对应初始 TRS2 颜色通道；至少一个 key 匹配初始通道的 track 为 5,371/5,437。type 24 为 9,849/9,977 条 track、19,242/19,427 个 key，所有 key 都在 `0..1`，所有目标节点都有 material alpha，初始 alpha 匹配为 8,142/8,229；仍作为独立 opacity/alpha 动画通道保留。Ras 每条有 4 个 vertex color，multiPos 全 0，multiSize 全 1。 | 继续确认颜色字段在渲染中的准确用途，尤其是 vertex color 与 alpha/opacity 的运行时混合规则；颜色/alpha 初始值匹配不是 100%，不能写成初始化规则。 |
| `DATA` | 空 payload 与 `ParamLow` | full survey 中日文 314 个 DATA 块、EN 322 个 DATA 块均无字段、无 trailing bytes；每个 `DATA.ParamLow` 均等于同一区间内后续 `CIMG+CNUM+CRFD+CSLI` 块数，覆盖日文 314/314 与 EN 318/318 个场景。`CREF/SLIC/TEXT` 不计入该数。`ParamLow == CIMG image cast count` 只覆盖 204/314 与 206/318。 | 将 `ParamLow` 记录为已确认结构计数：后续 primary resource block count（按 tag 集合 `CIMG/CNUM/CRFD/CSLI`）。运行时资源表语义仍不强行命名。 |
| `SCN` | `0x04/0x10/0x11/0x40/0x41` | 已补齐 `Scn*` full-survey 聚合。JP/EN 均为每场景 1 个 `SCN` 块，共 314/318；字段顺序固定为 `0x03 String, 0x10 UInt16, 0x11 UInt16, 0x04 Color32, 0x40 UInt16, 0x41 UInt16`，`ParamHigh=6`、parsed field count 6、trailing bytes 0。`ParamLow`、`0x10` 与同场景 `LAYR` 块数全量一致；JP 为 `1` x314，EN 为 `1` x316、`3` x2，两个 `3` 场景为 `MM_UI_CommonWindow.sbscene` 与 `MM_UI_NameEntry.sbscene`。`0x04` 为 34 种 Color32 raw hex；`0x11=0`、`0x40=1080` 全覆盖，`0x41=1080` 为 287/291、`450` 为 27/27。 | 保留为 raw structure/count-like/size-like 关系；`0x04/0x10/0x11/0x40/0x41` 不提升为背景色、图层数、视口或分辨率等运行时语义。 |
| `LAYR` | `0x20/0x21/0x22` | 已新增 `Layer*` full-survey 聚合。JP 为 314 个 `LAYR` 块且每场景 1 个；EN 为 322 个块，其中 `MM_UI_CommonWindow.sbscene` 与 `MM_UI_NameEntry.sbscene` 各含 3 个。字段顺序固定为 `0x03 String, 0x20 UInt32, 0x21 UInt16, 0x22 UInt16`，trailing bytes 为 0。`0x20` 只见 `0/0x100`，JP 为 94/220，EN 为 97/225，bit 只有 8。按 scene 对 `0x21` 求和等于 NODE 总数，JP/EN delta 均为 0；`ParamLow - 0x22` 为 1 覆盖 JP 313/314、EN 321/322，唯一 `2` 例外在 `MM_UI_Entry__MM_UI_Select_EntryName_ALL.sbscene`。 | 保留为 raw structure/count-like 关系；`0x20` 继续按 shared packed state word 处理，不命名运行时 flag；`0x21/0x22` 不提升为业务图层计数语义。 |
| `NCAT` | 节点分类 | 语义层已聚合所有 `NCAT` 块并按全局 NODE/TRS2 index 绑定；full survey 中 `NCAT.0x0E` 记录数等于 NODE 数量的场景为 314/314 与 318/318。非零分类记录分别为 2190 与 2260，category 只见 `0..8`，已在格式文档列出 JP/EN 分布。detail record 输出日文/EN 分别为 11845/12076 条，全部带 `0x0E` 分类；primary `0x03` kind 包含 `(none)`、`extparamdata`、`fontparamdata`、`mkrubytextdata`、`fontslot`、`fontnlo`。已新增 `NcatKind*`/`NcatTypeByte*` full-survey 交叉聚合：`(none)` 全部为 type `?` + category `0`；非空 kind 可跨多个 category，`extparamdata/fontparamdata/mkrubytextdata` 主要为 type `0x9`；`fontslot/fontnlo` 主要为 type `0x5`。`NCAT.0x0F` 不是单纯参数字符串，字段目录中已见 `String/RawByte03/Int32/Float32` 混合 payload；现有 `NcatParameterStringCounts` 只统计语义层字符串子集。 | 保留数组和附加字段；分类值、kind 字符串、type byte、`0x0F` 参数 payload 的运行时语义仍未确认。当前交叉统计只作为结构绑定证据，不命名具体 UI 功能。 |
| `CRFD` | raw resource record 结构 | 已新增 `CrfdRecords` 只读模型。full survey 中 CRFD 出现在日文 36 个文件/614 块、EN 37 个文件/622 块；DATA following tag 中 CRFD 合计同为 614/622。已观察 CRFD 均为 `ParamLow=0 / ParamHigh=7`，字段集合固定为 `0x51/0x90/0x91/0x92/0x93/0x94/0x95`；`0x51` 在 614/614 与 622/622 中均落在本文件 NODE index 范围内；`0x93=-1`、`0x94=0`、`0x95=0` 在当前 full survey 恒定。只读模型/JSON/Markdown/inspect 现保留 `0x90/0x91` raw hex，并输出 `0x94` 分布。新增 `CrfdField90Field91Counts` / `CrfdField90Field91Field92Counts` 后，JP/EN 均为 94 个 `0x90|0x91` 组合，且每个组合只对应一个 `0x92` 值；唯一 JP/EN 计数差异为 `MM_UI_NameEntry|Reference_Player_Name_Parts|2` 的 8/16。 | 只确认块结构、字段集合、值分布、`0x51` 范围和 `0x90|0x91|0x92` 稳定组合。`0x51` 仅作为 NODE index candidate；`0x90/0x91` 仅作为 source scene/resource name candidate；`0x92` 与恒定字段的运行时语义仍 unknown。 |
| `CSLI/SLIC` | slice resource 结构 | 已新增 `SliceCasts` 只读模型。full survey 中日文/EN 均为 26 个 CSLI、106 条 CSLI 后续 CREF、108 条 SLIC record；`CSLI.0x44 == 后续 CREF records` 为 26/26，target index in range 为 26/26。`CSLI.0x44 == SLIC records` 仅 24/26，两个小框资源有额外 `SLIC.0x83=2` record。`SLIC.0x83` 分布为 `0x0:10, 0x1:23, 0x2:14, 0x3:61`。新增 compact tail cross survey 显示 CSLI target node 当前均为 `NODE.0x30=0xF02/display=true/non-CIMG target`；`SLIC.0x37=#FFFFFFFF`、`0x38=#FF000000`、每条 `0x39` 为 4 个 Color32。只读模型/JSON/Markdown/inspect 现逐记录暴露 `CSLI.0x40..0x43/0x80..0x87`，并保留 `SLIC.0x37/0x38/0x39` 的 Color32 raw hex。 | 只确认块绑定、`0x44` 与 CREF 记录数、target index 范围和 SLIC record raw 字段。`CSLI.0x51` target 角色、`SLIC.0x83/0x40/0x41/0x45/0x37/0x38/0x39` 的运行时语义仍未确认。 |
| `CNUM/TEXT/SLIC` | compact tail / `type=0x00` payload | 已结构化解析，full survey 中日文 314/314 与 EN 318/318 个场景均 `ScenesWithWarnings=0`，未知 VTBF type code 为空。已新增 `CnumRecords` 与 `TextRecords` 只读模型。`CNUM` 在 JP/EN 均为 217 条，后续 CREF records 均为 2422，`CNUM.0x44` vs following CREF records 为 217/217，`0x51` in range 为 217/217；zero marker field id 分布均为 `0x02:8, 0x0A:19, 0x0C:20, 0x0D:170`。新增 raw/cross survey 显示 `CNUM.0x48=32768` 覆盖 217/217，`0xA0` 有 24 个 raw int 取值，`0xA1` 有 42 个 raw 字符串值，且 `0xA1|0x44` 与 `0xA1|CREF record count` 聚合一致；`0xA1` raw bytes 为 217/217 ASCII-only + strict UTF-8 valid。`CNUM.0x40/0x42/0x43`、`0x39 x4`、`0xA2..0xAF` 现已在只读模型与 JSON/Markdown/inspect 中按 float、Color32 raw/display、raw byte/int、`0xAE` float values、`0xAF` packed values 逐记录暴露。`TEXT` 在 JP/EN 为 105/131 条，`0x7A` present 为 105/131，zero marker 均为 `0x00`；`0x78|0x79` top 为 JP `23|-1:44`、EN `23|-1:70`；`TEXT.0x7A` strict UTF-8 invalid 为 JP 97/105、EN 123/131，invalid 项全部符合 Shift-JIS byte-shape with non-ASCII，且 JP/EN 分别为 `validShiftJis=105/105`、`validShiftJis=131/131`。模型/JSON/Markdown/inspect 现逐记录暴露 `field7AShiftJis` 只读解码值，full survey 新增 `TextField7AShiftJisStringCounts`。`TEXT.0x33` 已在模型中以 raw hex 与 vector 暴露，`TEXT.0x7B` 已在模型中以 raw hex 与 packed values 暴露。`SLIC` 已按记录块解析 `FE 00`/`FD 00` marker。 | 只确认字段边界、raw 值、后续 CREF 计数关系、索引范围、raw-byte 编码形态和 `TEXT.0x7A` raw content 可严格按 CP932/Shift-JIS 解码；`field7AShiftJis` 仅为只读机械解码证据。`ZeroLengthMarker`、`CNUM.0x40/0x42/0x43/0x39/0xA0..0xAF`、`TEXT.0x33/0x7A/0x7B/0x7C/0x41`、`SLIC` 各字段的运行时语义仍未确认。当前 `TEXT.0x7A` 不确认最终文本编码。 |
| `CIMG` | `0x44` 两个值 | 已确认是 primary/secondary 两组 CREF 记录数；full survey 中 7063/7063 与 7228/7228 个 image cast 均与后续 CREF 组记录数匹配。 | 当前样本内已确认两组结构；跨版本或新增资源形态是否仍固定为两组另行验证，解析器同时保留兼容字段。 |
| `CIMG` | `0x45` 两个值 | primary/secondary 组内引用 index raw 值；full survey 中 7315/7315 与 7454/7454 个非空 CREF 组均在范围内，无 out-of-range，empty group non-zero 为 0/0。运行时代码 `sub_88EC90` 将其读取为两个组内 index，`sub_7CE8B0` 按 `CIMG.0x44` 两组 count 对其做范围校验。已新增 `Cimg45GroupIndexCounts`、`Cimg45GroupCountIndexCounts`、`Cimg45NonZeroGroupCounts` 和 `Cimg45NonZeroSamples`：JP 非零 group 为 131（primary 127、secondary 4），EN 为 124（primary 120、secondary 4）。JP primary index 分布为 `0=6936, 1=86, 2=11, 3=14, 5=5, 6=6, 9=4, 64=1`，secondary 为 `0=7059, 1=4`；EN primary 为 `0=7108, 1=83, 2=8, 3=14, 5=5, 6=6, 9=4`，secondary 为 `0=7224, 1=4`。`ImageVariantGroupCimg45FirstKey*` 显示它与 type 18/19 最早 key 经常一致但非全覆盖：JP primary 1533 match / 258 mismatch、secondary 332 match / 1 mismatch；EN primary 1534 match / 257 mismatch / 1 multi-CIMG target、secondary 332 match / 1 mismatch。 | 已确认是两组 CREF 的组内静态 index；不能全局命名为动画初始 key、`selected/default/current` 或其它高层状态。 |
| packed state | `CIMG.0x48` / `CNUM.0x48` / `CSLI.0x80` / `LAYR.0x20` / `TEX.0x62` / `SLIC.0x83` | `CIMG.0x48` 已输出 full value、bit 分布、节点 flags/group/display/resource 交叉统计和 bit 共现表；full survey 中观察到 bit `0/1/4/5/6/7/8/9/11/12/13/15/20/21/22/23`。运行时代码进一步确认它不是 CIMG 私有 flags：`sub_88EC90`、`sub_88E120`、`sub_88F740`、`sub_88BDB0`、`sub_88C670` (`LAYR.0x20`)、`sub_88E7D0` 复用同一组 bitfield decoder。已确认 mask 包括低位 `0x0F/0x10/0x20/0xC0/0xF0`、中位 `0x100/0xF00/0x7800/0x18000`、CIMG 高位 `0xF00000/0x01000000/0x02000000`。CLI full survey 已新增统一 `SharedPackedStateOwner*` 聚合：JP/EN owner 总数为 `CIMG.0x48=7063/7228`、`CNUM.0x48=217/217`、`CSLI.0x80=26/26`、`LAYR.0x20=314/322`、`SLIC.0x83=108/108`、`TEX.0x62=2906/2922`；`CNUM.0x48` 恒为 `0x8000`，`CSLI.0x80` 只见 `0x8000/0x8001/0x8002`，`LAYR.0x20` 只见 `0/0x100`，`SLIC.0x83` 只见 `0..3`，`TEX.0x62` 只见 `0x110/0x0`。CIMG full survey 中 bit 15 为 7,039/7,191，高覆盖但不全局必有；`0+22` 共现为 1,624/1,639，`20+23` 共现为 559/567。 | 文档已改为 shared packed state word / runtime bitfield layout。后续要继续沿渲染状态消费链确认业务语义；当前不能把 bit 0 命名为 additive/blend，不能把 bit 15 命名为 image-cast marker，也不能把 TEX bit 4/8 命名为 wrap/filter/layout。 |
| `CROP/CREF` | compact int16-vector record | `CROP.0x65` 与 `CREF.0x49` 现在在只读模型中保留 `RawHex`。运行时 compact parser 显示 `0x45` 是 `0x40 | Int16`，payload 首字节是 component-count selector，不是业务 kind/enum：`CREF.0x49` 的 selector `1` -> 3 个 int16，由 `sub_88F380` 读成 `(textureListIndex, textureIndex, cropIndex)`；`CROP.0x65` 的 selector `2` -> 4 个 int16，由 `sub_88C110` 读出并按 TEX width/height 归一化为 4 个 float。full survey 中 `CROP.0x65` selector 均为 `2`（79,801 与 80,217 条），所有 TEX atlas 的 declared crop count 均匹配实际 CROP 数（2,906/2,906 与 2,922/2,922）；crop size 无非正数，越过 atlas 坐标边界的 crop 为 259/191，并已输出 `CropRectOutOfAtlasBoundsReasonCounts` 与逐场景 sample。`CREF.0x49` 现在按 CIMG/CNUM/CSLI 全 owner 聚合，selector 均为 `1`（19,597 与 19,734 条），owner 分布为 JP `CIMG=17,069, CNUM=2,422, CSLI=106`，EN `CIMG=17,206, CNUM=2,422, CSLI=106`。CREF texture/crop index in range 为 JP 19,595/19,595、EN 19,734/19,734；JP 另有 2 条 `textureListIndex=65535` / missing texture raw 特例，均定位为 CIMG owner，raw 均为 `01FFFFFFFFFFFF`。 | 已确认 compact 布局和 tuple 含义；JP `65535` 特例含义、越界 crop 的运行时裁剪/采样规则仍 unknown。 |
| `TRK` | flags bit / 字段顺序 | 已按 base byte `0x13/0x23/0x33/0x43` 归类存储类型；低 nibble为 `0x3`，storage nibble `1/2/3/4` 分别对应 `KEY.0x5B` 的 Float32/Int32/Bool/PackedAngleCandidate。full survey 中 base byte 仍只出现 `0x13/0x23/0x33/0x43`；`extra mask` 只出现 `0x0/0x100`，其中 `0x100` 为 3063 与 3066 条，且不改变 key value 存储类型。已新增 `TrackFlagExtra*` 交叉聚合：`0x100` 出现在 123/124 个场景，base 分布 JP/EN 为 `0x13=1980/1983, 0x23=177/177, 0x33=51/51, 0x43=855/855`；CIMG target 为 1818/1821，非 CIMG target 为 1245/1245。新增 `TrackFieldSequenceCounts` 后，JP/EN 的 94,611/95,558 条 TRK 字段顺序均固定为 `0x53,0x57,0x54,0x58,0x59`。 | 继续确认低 nibble `0x3` 与 extra mask `0x100` 的 bit 语义；`0x100` 现在只能记录为 raw extra mask，不能命名为 action-local/special effect。base byte high nibble 先按 storage 处理。 |
| `TRK` | track type 部分 ID | `2/3/4/8/18/19/21/22/23/24/25/26/27/28` 已提升候选命名或运行时范围约束。已新增 `TransformTrack*` full survey 聚合：JP/EN transform track 为 68,482/69,174 条、key 为 124,712/125,579 个；type `0/1/5/6/7` 均能绑定目标 `TRS2` 初始通道，至少一个 key 匹配初始通道为 64,375/65,001，mismatch 为 3,543/3,609；type `2/3/4/8` 无可比初始通道，但全部 key 都等于候选默认值：`TranslateZ=0`、`RotateX=0`、`RotateY=0`、`ScaleZ=1`。type 18/19 的语义层命名已收敛为 `PrimaryImageVariantIndexCandidate` / `SecondaryImageVariantIndexCandidate`；legacy `ImageVariant*` 字段只保留 type 18 的 primary+secondary 合计宽松统计。新增 `ImageVariantGroup*` 分组校验确认 `18 primary` 为 JP/EN 1,791/1,792 条 track、6,986/6,988 个 key，`19 secondary` 两组均为 333 条 track、9,769 个 key，全部目标节点都有 CIMG 且无 missing/non-int/out-of-range。`ImageVariantGroupCimg45FirstKey*` 进一步显示最早 key 与 `CIMG.0x45` 不完全等价：`18 primary` JP/EN 为 1533/1534 match、258/257 mismatch（EN 另有 1 个 multi-CIMG target）；`19 secondary` 两组均为 332 match、1 mismatch。type 21/22/23 与 `TRS2.0x37` 材质色 RGB 通道关联，type 25/26/27/28 与 `TRS2.0x38` illumination RGBA 通道关联；full survey 中这些颜色 key 全部在 `0..1`，目标初始通道均存在。type 28 在 JP/EN 均为 29 条 track、31 个 key，全部目标有 illumination alpha 初始通道且全部 track 至少一个 key 匹配。type 24 与目标初始材质 alpha 有匹配证据，且用于 root/effect/action opacity。 | 继续用运行时代码、播放行为或新增 pattern 确认候选名；type 18/19 的 validator 分组和静态范围关系已确认，但播放时如何与 `CIMG.0x45` 和其它状态组合仍未确认；`CIMG.0x45` 不能作为默认/当前/选中状态命名依据；transform/颜色/alpha track 只确认通道关联、存储和值域，不确认运行时混合、初始化或 3D 轴规则；type 28 虽已补齐 full-survey 通道匹配证据，但仍不提升为已确认渲染 alpha 规则。 |
| `KEY` | `0x5C/0x5D/0x5E` / frame 序列 | `0x5C` 为 interpolation 候选；full survey 未发现 `TRK.0x57` key count mismatch。已新增 `KeyInterpolation*`/`KeyTangent*` 交叉聚合：JP/EN 的 `0x5C` 分布为 `Linear=107658/108687, Spline=51674/51787, StepOrConstant=27448/27528`；非零 tangent 为 9757/9777；`0x5D != 0x5E` 为 123/123 个 key、9/9 个场景，track type 分布为 `RotateZ=104, TranslateY=11, ScaleX=4, ScaleY=4`，extra mask 为 `0x0=102, 0x100=21`。新增 `KeyFieldSequenceCounts` 与 frame range survey 后，JP/EN 的 KEY 字段顺序只有 4 种，差异仅为 `0x5B` type；全部 track 的 key frame 为非递减，first frame 均等于最小 key frame，last frame 等于最大 key frame 为 91,185/92,114，last frame 大于最大 key frame 为 3,426/3,444；重复 frame 仅 2/2 条，均来自两个 UI 场景的 `FadeIn -> cover` type 24 alpha 轨道。 | 继续确认 Hermite/Bezier tangent 语义、重复 frame 的运行时处理、last frame 大于最大 key frame 时的播放/hold 规则；`0x5D/0x5E` 不能写成全局相等，也不能写成只属于 Otohime RotateZ。 |
| `KEY` | `0x5B type 0x0B` | 已新增 full survey packed-angle 聚合。JP/EN 分别为 16,265/16,305 条 track、45,602/45,690 个 key，只出现在 track type `3/4/5`；type 5 分别占 16,100/16,140 条 track 与 45,437/45,525 个 key。raw distinct 两组均为 1,092，range 均为 `-75548..83740`；top raw 包括 `0`、`910/-910`、`546/-546`、`1820/-1820`，按候选公式 `degrees = raw * 180 / 32768` 约为 0、±5、±3、±10 deg。raw 超出 16-bit signed 范围，不能写成 i16 angle。 | 继续用运行时代码、播放行为或新增 raw pattern 确认公式、wrap/clamp 与多圈行为；不要将该公式套用到 `CAM.0x14 = 0x1FFF` 这类 flags-like/unknown 字段。 |
| 动作系统 | 换装/动作状态机 | 已从 `Display`、`PrimaryImageVariantIndexCandidate`、`SecondaryImageVariantIndexCandidate`、`AlphaOrOpacity` 生成状态/开关轨道摘要；播放器逻辑仍未还原。 | 需要运行时代码、播放日志或 writer roundtrip 才能确认状态机和默认状态选择规则；当前摘要不作为正式运行时逻辑。 |

## svo / YABX 未知项

| 区域 | 未知项 | 当前状态 | 后续解析动作 |
| --- | --- | --- | --- |
| AVTS header | 除 magic/count 外字段 | 已输出 header size/table offset/entry size、unknown bytes/words、word class 汇总，并新增 offset/value、offset/class、relation 与 payload location full-survey 聚合。Ras/Chiffon/Otohime 均有约 50 个非零 unknown 字节。full survey 中 47+47 个 SVO 均存在 header unknown 字节，日文合计 2576 bytes、EN 合计 2567 bytes；word class 汇总日文为 in-file-offset 440、pointer/residue 136、small-scalar 179、unknown 298、utf16 36，EN 为 448/134/185/287/32。offset/value 聚合显示没有任何 unknown word offset 在 47 个 SVO 内出现全覆盖非零常量；唯一全组稳定 word 为 `0x70=0`。新增 relation 聚合显示 `0x64/0x68` 只见 out-of-file 或 pointer/residue，而 `0x6C` 在两组各 47/47 都落入 payload：DDS 46、YABX 1；其 payload entry index 和相对偏移仍变化。 | 继续收集 SVO 样本比较；当前分类、relation 和高频 raw 值仅作排查，不作为 confirmed 语义。 |
| AVTS directory | `+0x210..+0x3FF` | full survey 中 47/47 与 47/47 个 SVO 均无 directory reserved 非零项。 | 等待非零样本。 |
| YABX header | magic/version/payload length 后 4 字节 | full survey 中 `declaredPayloadLength == YABX entry length - 16` 在 JP/EN 两组各为 47/47，mismatch 为 0。Ras/Chiffon/Otohime header 最后 4 字节分别为 `0xA4E986BC`、`0x69AE571D`、`0x06E77BC1`，仍只命名为 `headerHashCandidate`；JP/EN 组内各 47 个值均不重复，合并 94 个样本 union 为 53 个值、intersection 为 41 个值。脚本排查 CRC32、CRC32C、Adler32、FNV1a32 的常见 payload 输入变体均为 0/94 匹配。 | `declaredPayloadLength` 可按 header 后 payload 长度处理；最后 4 字节保留候选名，继续确认是否为非典型 hash/checksum、GUID/构建残留或其他 ID。 |
| YABX schema | 字段描述符 3 字节 | 已输出 raw descriptor、descriptor offset、候选 `OwnerDependent/Int32Like/ObjectReference`、descriptor usage evidence 和 descriptor 字节分布；full survey 两组各 1927 个 descriptor 第三字节均为 0。raw counts 两组均为 `000000=564, 000400=47, 010000=376, 010200=94, 010400=846`；flags 为 `0x0=611/0x1=1316`，valueKind 为 `0x0=940/0x2=94/0x4=893`。已新增 full survey 的 `owner.field + raw + object payload kind` 聚合；两组 raw-to-payload-kind 均为 `000000->ReferenceList 423/String 141`、`000400->Int32 47`、`010000->ReferenceList 47/String 329`、`010200->ObjectReferenceId 94`、`010400->Int32 846`。`valueKind=0x00` 不能命名为 string/inline，解析器已收窄为 `OwnerDependent` label；raw descriptor 需要结合 owning type 判断实际容器。 | 继续确认 flags/valueKind/reserved 的准确命名；第三字节目前只确认 observed zero，不命名语义。 |
| YABX object refs | 引用 ID 规则 | full survey 中 JP/EN 两组各 47/47 个 SVO 均推断 `referenceBase=0x2711`；对象已补 `referenceId`，引用字段已反查目标对象。新增 resource binding 校验后，两组各 281/281 个 resource record 均满足 `Texture._image == paired Image reference id`。 | 当前 `Database / VertexDeclaration / VertexElement / Texture / Image` resource skeleton 内可按 `referenceId - 0x2711` 静态反查；其它 YABX 版本或新增对象骨架是否固定同一 base 仍不外推。 |
| YABX objects | 复杂 payload 通用解码 | full survey 中 47/47 与 47/47 个 SVO 的 YABX object payload 均无 unparsed bytes。已见对象类型仍为 `Database/VertexDeclaration/VertexElement/Texture/Image`。新增 object skeleton 校验：两组各 47/47 个 SVO 均满足 object count = `6 + 2 * DDS count` 和对象顺序 `Database, VertexDeclaration, VertexElement, VertexDeclaration, VertexElement, VertexElement, (Texture, Image) * DDS count`；两组 object totals 均为 `Database=47, VertexDeclaration=94, VertexElement=141, Texture=281, Image=281`。resource record count == DDS count、Image dataSize == AVTS dataLength、Image width/height == DDS width/height 均为 281/281。 | 当前只确认 full survey 的 resource skeleton / pairing；继续用更多类型样本泛化 list/string/reference 容器，不命名 `Database` 列表用途、`VertexElement` 枚举或 `_format` 语义。 |
| `stevia::Image._format` | 格式枚举 | full survey 中 `_format=1` 覆盖全部已见 DDS pixel formats：DXT5、A8R8G8B8、DXT1、A4R4G4B4、R5G6B5、R8G8B8。说明 `_format=1` 不能命名为 DDS pixel format 枚举。解析器已支持这些已观察格式切图。 | 继续收集更多格式样本；区分 YABX format code 与 DDS pixel format。 |
| 写回 | `.svo` 重新打包 | 未实现。 | 只读解析稳定后再设计 writer。 |

## 本轮已执行

1. 已解析并输出 AVTS header 信息。
2. 已解析并输出 AVTS 目录项保留区非零统计。
3. 已解析并输出 YABX `headerHashCandidate`。
4. 已为 YABX object 推断并输出 `referenceId/referenceBase`。
5. 已更新 `inspect-svo`、`manifest.json` 和 `docs/svo-resources.md` 等文档/输出。
6. 已运行 `build/test/dump/extract-images` 验证。
7. 已补充 inspect/Markdown 的块参数、NODE flags、TRS2 字段、DATA/NCAT、KEY tangent 统计。
8. 已补充状态/开关轨道表，并对 type 18 image variant key value 做 CIMG crop reference 范围校验；后续运行时代码已将 validator 关系收窄为 type 18 对 primary 组、type 19 对 secondary 组。
9. 已补充 `CIMG.0x45` primary/secondary 组内引用 index 分布，并确认 Ras/Chiffon/Otohime 中非空 CREF 组均没有 out-of-range；默认/当前/选中三种高层命名仍未确认。
10. 已补充 `CIMG.0x48` bit 分布表；确认 Ras 只出现 bit 0/15/20/21/22/23，其中 bit 15 覆盖 Ras 全部 CIMG。后续 MM_CH 9 场景 survey 已把 bit 15 修正为高覆盖率候选而非全局必有；再后续运行时代码复核又将 `0x48` 修正为跨资源复用的 packed state word，不再按 CIMG 私有 bit 语义命名。
11. 已补充 `TRK.flags` nibble/storage 证据表、统一 Track type 证据表和非零 tangent 样例；确认 base byte high nibble 对应 `KEY.0x5B` 存储类型，Ras 阶段 type 18 为 `0x23 + Int32` 且值域为 `0..3`，后续 full survey 扩展到 `0..15`。
12. 已补充 YABX object payload 覆盖统计和 object field kind 分布；补齐 `stevia::VertexDeclaration` 的 Resource 基类字段，使 Ras 的 14 个 YABX object 全部无剩余未解析字节。
13. 已补充 AVTS header unknown word 候选分类和 YABX descriptor usage evidence；确认部分 descriptor raw 值如 `000000/010000` 不能脱离 owning type 单独决定 string/list 容器。
14. 已补充 sbscene 字段目录输出；`out/ras.md` 现在按 `tag + field id + type` 输出 82 行字段证据，包括 flags 字段的 hex 分布和 `NODE/TRS2/NCAT` record marker 分布。
15. 已新增 packed angle candidate 输出/命名约定，并更新 `docs/sbscene-format.md`、`docs/animation-system.md`、`docs/ras-sample.md`：`0x0B` 旧称 `Int32/Float32/PackedFloat32` 不再作为确认结论；在 `TRS2.0x32` 和旋转 `KEY.0x5B` 上下文中按 signed fixed-angle raw int 候选记录，公式候选为 `degrees = raw * 180.0 / 32768.0`。
16. 已新增 `VtbfBlock.ParamRawHex`，在 JSON、Markdown 与 inspect 中保留根块/线性 `vtc0` 参数的原始 4 字节；Ras/Chiffon/Otohime 根块均输出 `0x0100004C`，不再只剩 low/high 拆分值。
17. 已为每个 image cast 新增 packed state bit 输出；legacy JSON 字段名为 `imageCastFlagBits`。已扩展 `CIMG.0x48` full value/bit 交叉统计与 bit 共现表；Ras 中 bit 0 是 bit 22 的子集，bit 23 是 bit 20 的子集，bit 0 与 multi/secondary CREF、非零 `0x45` group index 无交叉。这些是 CIMG 样本共现关系，不提升为 bit 语义。
18. 已加入 Chiffon 第二样本并生成 `out/chiffon.*` 与 `out/chiffon-svo-extract/`；确认 `.sbscene` 无未知 type code，`.svo` 14/14 YABX objects 全覆盖。Chiffon 暴露 `TRK.flags extra=0x100`，解析器和报告已改为 base byte / extra mask 拆分，并新增 extra mask 上下文表；`0x100` 的 6 条 track 全部属于 `Action_Wait3 -> smile`。
19. 已新增 `NodeInfo.flagBits` / `NODE.0x30` bit 分布输出，并在 Ras/Chiffon 报告中交叉统计 CIMG、动画绑定和初始 display；同步修正 CIMG bit 共现文案避免把 Chiffon 标成 Ras，补充合成测试样本的 NODE flags 覆盖。
20. 已加入 Otohime 第三样本并生成 `out/otohime.*` 与 `out/otohime-svo-extract/`；确认 `.sbscene` 63,501 blocks、501 nodes、28,167 tracks、无未知 type code，`.svo` 12/12 YABX objects 全覆盖。Otohime 新增 `NODE.0x30=0x901`，将 `TRK.flags extra=0x100` 扩展到 63 条，并证明 `CIMG.0x48` bit20/23 不能按固定子集关系命名。
21. 已将 `TRS2` 颜色字段按 `A,R,G,B` 候选暴露；结合 Ras heart 节点和 Otohime `momo_circle_*`，把 track type `21/22/23` 提升为 `MaterialColorR/G/BCandidate`，type `25/26/27/28` 提升为 `IlluminationColorR/G/B/AlphaCandidate`，并同步更新文档和输出。
22. 已在 Markdown/inspect 输出里新增“颜色 track 证据”小节，自动对比 `MaterialColor` / `IlluminationColor` 初始通道和对应 track key 值，便于后续样本继续验证。
23. 已新增 “Alpha/Opacity track 证据” 输出：Ras/Chiffon/Otohime 的 type 24 分别为 58/34/37 条 track，material alpha 匹配为 56/58、34/34、37/37；确认它与 `TRS2.0x37` alpha 有样本匹配证据，但仍覆盖 root/null/control 和 effect 节点，暂按独立有效 opacity/alpha 动画通道处理。
24. 已在 Markdown/inspect 资源摘要中新增 `CROP.0x65` / `CREF.0x49` compact record 首字节分布；三份样本合计 `CROP selector=2` 为 854/854，`CREF selector=1` 为 993/993。后续运行时代码确认该首字节是 component-count selector，不是业务 kind。
25. 已在 DATA/NCAT 输出中新增 `imageCasts` 与 `matchesImageCasts`；Ras/Chiffon/Otohime 的 `DATA.ParamLow` 分别等于 304/207/380 个 image cast，且 DATA 块均无字段和 trailing bytes。后续 full survey 已将该结论修正为：`DATA.ParamLow` 全局对应后续 `CIMG+CNUM+CRFD+CSLI` 块数，单纯 CIMG 只适用于部分场景。
26. 已在 Markdown/inspect 资源摘要中新增 `CIMG.0x44/0x45` 值域校验；三份样本合计 `0x44` 记录数匹配 891/891，`0x45` 组内 index 在 898/898 个非空 CREF 组内都未越界，非零 index image cast 合计 16 个。
27. 已复核三份样本的 `NCAT.0x0E`：记录数 428/297/501 均等于 NODE/TRS2 数量，取值分布均为全 0；同时 `NCAT.ParamHigh = 2 * recordCount + 1` 与记录 marker 数量匹配。
28. 已在 Markdown/inspect 的 KEY 插值小节新增 `0x5D != 0x5E` tangent mismatch 分布和样例；三样本阶段 Ras/Chiffon 没有 mismatch，Otohime 有 82 个 mismatch，按 interpolation 为 Linear 14、Spline 68，全部属于 type 5 `RotateZ` 的 64 条 track。后续 full survey 已扩展为 123/123 个 mismatch、9/9 个场景，不能再写成只属于 Otohime RotateZ。
29. 已在 Track flags extra mask 输出中新增 node flags、node group、CIMG targets、display=false 交叉统计；确认 Chiffon `extra=0x100` 为 `0xE01/smile/(ungrouped)` hidden CIMG 节点 6/6，Otohime 为 CIMG 63/63、`0xF01` 61/63、display=false 2/63，主要 group 为 `eye` 48、`puru` 13、`arm` 2。
30. 已在 `inspect-svo` 输出中新增 AVTS header unknown word class 汇总、目录 reserved 汇总和 YABX descriptor 字节分布；确认 Ras/Chiffon/Otohime 合计 14 个 AVTS 目录项的 `+0x210..+0x3FF` 区域全为 0，三样本合计 123 个 YABX descriptor 第三字节全为 0。
31. 已新增 `survey` CLI，批量输出 sbscene/SVO JSON 证据；MM_CH 9 场景与 7 个 SVO 均解析成功，`RootParam=0x0100004C` 为 9/9，`CIMG.0x44` 匹配 1676/1676，`CIMG.0x45` active groups 1680/1680 in range，`TRK.0x57` mismatch 为 0，SVO YABX unparsed bytes 为 0。
32. 已补充 compact `type=0x04` 解析为 1 字节 raw 值，避免 Shama `NCAT` 在 `0x0D/0x0F` 附加字段处停止；Shama `NCAT.0x0E` 现在完整展开为 416 条，分类分布 `0:225, 1:86, 2:105`。
33. 已补充 DDS A8R8G8B8 格式识别/解码；`MM_CH_gage.svo` 的两个 texture 分别为 A8R8G8B8 与 DXT5，`extract-images` 对 gage_front 成功输出 89 个 crop PNG。
34. 已对 surfboard/surfboard_EN 做 full survey：314/314 与 318/318 个 sbscene 全部解析成功，无未知 VTBF type code；47/47 与 47/47 个 SVO 全部解析成功，AVTS directory reserved 非零为 0，YABX unparsed bytes 为 0。
35. 已补充 DDS DXT1、A4R4G4B4、R5G6B5、R8G8B8 格式识别/解码；full survey 的 SVO texture format 不再出现 `UnknownFourCC(00000000)`。
36. 已用 `extract-images` 验证 UI SVO 新格式切图：Announce 输出 6 张 atlas/88 个 crop，ResultInfo 输出 8 张 atlas/53 个 crop。
37. 已把 `survey` 的 aggregate 扩展为 `NodeFlagBitCounts`；full survey 输出 NODE bit 分布，确认 observed bits 为 `0/1/2/8/9/10/11/15/16`，并保留 exact flag 分布。
38. 已补充 compact `type=0x03` 解析为 `RawByte03` 单字节 raw 值；`MM_CH_gage__Reference_LV` 的 `CNUM.0x00A7/0x00A9..0x00AC` 已能进入字段目录，`0x00A7` 样例值为 239/244/248，`0x00A9..0x00AC` 为 0。该类型只确认长度和值，不命名语义。
39. 已新增 `NodeCategoryDetails` 语义模型、inspect/Markdown 表格和 survey aggregate：聚合全部 `NCAT` 块后，full survey 中日文/EN 的 NCAT detail records 为 11845/12076，withCategory 为 11845/12076，withoutCategory 为 0；primary kind 分布已输出。survey aggregate 为避免大小写键冲突会统一小写，单条 detail record 仍保留原始字符串。
40. 已新增 survey warning 聚合 `ScenesWithWarnings` 与 `WarningKindCounts`；当时剩余 sbscene warning 只来自 `CNUM/TEXT/SLIC` 的 `type=0x00` 或尾部 payload，SVO full survey 仍为 47/47 与 47/47 全解析成功且无 YABX unparsed bytes。
41. 已修复未知 compact 字段保留逻辑：解析失败时 cursor 回退到字段头，`TrailingBytes` 现在包含未知 `field id/type` 本身，不再只保留其后的 payload。新增单元测试 `UnknownCompactFieldKeepsHeaderInTrailingBytes`，并重新生成两份 full survey；warning 分类计数保持不变。
42. 已补充 compact `type=0x00` 为 `ZeroLengthMarker`，并允许 `CNUM/TEXT` 在 declared high count 之后继续解析可验证 compact 字段；已将 `SLIC` 纳入记录 marker 解析。重新生成 full survey 后 JP/EN 的 `ScenesWithWarnings` 均为 0，未知 type code 均为空；新增单元测试覆盖 `type=0x00`、补充字段和 `SLIC` 记录 marker。
43. 已修复多 `NODE/TRS2/NCAT` 记录块的全局 index 聚合：EN `MM_UI_CommonWindow` 和 `MM_UI_NameEntry` 的所有 `NCAT` 块现在都会进入语义模型，`NcatMatchesNodes` full survey 提升为日文 314/314、EN 318/318。新增单元测试覆盖跨块节点、TRS2 display 与 NCAT 分类绑定。
44. 已把 `DATA.ParamLow` 的结构计数从单纯 image cast 修正为后续 `CIMG+CNUM+CRFD+CSLI` 块数；CLI survey、inspect 和 Markdown 同时输出后续 tag 计数以及 `CIMG`、`CIMG+CRFD`、`CIMG+CNUM+CRFD`、`CIMG+CNUM+CRFD+CSLI` 的匹配情况。full survey 中最终组合为日文 314/314、EN 318/318，`CREF/SLIC/TEXT` 明确不计入。
45. 已新增 `CSLI/SLIC` 只读资源模型、Markdown/inspect 输出和 survey 聚合。full survey 中 JP/EN 均为 `SliceCasts=26`、`SliceCropReferences=106`、`SliceRecords=108`、`CSLI.0x44 vs CREF=26/26`、target index in range=26/26；`CSLI.0x44 vs SLIC=24/26`，因此不会把 `0x44` 写成 SLIC record 数。新增单元测试覆盖 `CSLI -> CREF -> SLIC` 绑定。
46. 已新增 `CRFD` 只读资源模型、Markdown/inspect 输出和 survey 聚合。full survey 中 JP/EN 为 `CrfdCount=614/622`、`Field51InRange=614/622`、`Field51OutOfRange=0/0`、`Field94NonZero=0/0`；`0x93=-1`、`0x94=0`、`0x95=0` 在当前样本恒定。后续补强新增 `Field90RawHex` / `Field91RawHex`，并让 Markdown/inspect 输出 `0x94` 分布。新增单元测试覆盖 `CRFD` 7 字段解析，重跑 `dotnet test` 为 18/18，通过 `dotnet build --no-restore`，并重新生成 Ras/Chiffon/Otohime、EN NameEntry、EN TitleFrame Large 和 JP/EN full survey 输出。
47. 已新增 `CNUM/TEXT` 只读资源模型、Markdown/inspect 输出和 survey 聚合。full survey 中 JP/EN 均为 `CnumCount=217`、`CnumCropReferenceCount=2422`、`CNUM.0x44 vs CREF=217/217`、`CNUM.0x51 in range=217/217`；`TextCount=105/131`、`TEXT.0x7A present=105/131`、`TEXT zero marker=0x00` 全覆盖。新增单元测试覆盖 `CNUM -> CREF` 与 `TEXT` raw record，重跑 `dotnet test` 为 20/20，通过 `dotnet build --no-restore`，并重新生成相关 sample dump 和 JP/EN full survey。
48. 已新增 SVO/YABX descriptor usage 的 full survey 聚合字段：`YabxDescriptorUsageCounts`、`YabxDescriptorRawUsageCounts`、`YabxDescriptorRawObjectKindCounts`。重新生成 JP/EN full survey 后两组均为 47/47 个 SVO 解析成功、`referenceBase=0x2711` 为 47/47、YABX unparsed bytes 为 0，descriptor usage 总数均为 1927；raw-to-payload-kind 聚合两组完全一致，但只作为容器观察证据，不提升为 descriptor 字节语义命名。
49. 已新增 SVO/YABX object/resource skeleton survey 聚合字段：`YabxExpectedObjectCountFromDds`、`YabxObjectCountMatchesDdsSkeleton`、`YabxObjectTypeOrderMatchesDdsSkeleton`、`YabxResourceRecordCountMatchesDds`、`YabxResourceTextureImageReference*`、`YabxResourceDataSize*`、`YabxResourceDimensions*`。重新生成 JP/EN full survey 后，两组各 47/47 个 SVO 都匹配 `6 + 2 * DDS count` 对象数和当前观察到的对象顺序；两组各 281/281 条 resource record 都匹配 DDS count、Texture->Image reference、dataSize 和 width/height。该结论只作为当前样本 resource skeleton，不提升为全版本规范。
50. 已新增 YABX header survey 聚合字段：`YabxDeclaredPayloadLengthMatchesEntryLength`、`YabxDeclaredPayloadLengthMismatchesEntryLength`、`YabxHeaderHashCandidateCounts`。重新生成 JP/EN full survey 后，declared payload length 两组均为 47/47 匹配；`headerHashCandidate` 组内各 47 个值不重复，合并 union 53 / intersection 41。额外脚本排查常见 CRC/Adler/FNV payload checksum 变体均为 0/94 匹配，因此最后 4 字节仍保持 unknown candidate。
51. 已新增 `KEY.0x5B type 0x0B` packed-angle candidate 的 full survey 聚合字段：`PackedAngleTrackCount`、`PackedAngleKeyCount`、`PackedAngleTrackTypeCounts`、`PackedAngleKeyTrackTypeCounts`、`PackedAngleRawCounts`、`PackedAngleDegreeCandidateCounts`。重新生成 JP/EN full survey 后，`0x0B` 只出现在 track type `3/4/5`，raw distinct 两组均为 1,092，raw range 均为 `-75548..83740`。该结果支持 signed int raw angle candidate，但不确认最终公式、wrap/clamp 或运行时插值语义。
52. 已新增 type 18 legacy `ImageVariant*` full survey CIMG 范围校验字段：`ImageVariantTrackCount`、`ImageVariantKeyCount`、`ImageVariantTracksWithCimg`、`ImageVariantTrackRangeMatches`、`ImageVariantKeysInRange`、`ImageVariantReferenceCountCounts`、`ImageVariantValueCounts` 等。重新生成 JP/EN full survey 后，JP 为 1,791 条 track / 6,986 个 key，EN 为 1,792 条 track / 6,988 个 key；两组均无 CIMG missing、非整数 key、越界 key 或 missing value。该静态校验只确认 type 18 key value 与目标 CIMG CREF 索引范围存在结构关系；运行时代码 `sub_7CE8B0` 进一步确认 type 18 使用 primary CREF count，type 19 使用 secondary CREF count，不确认播放状态机。
53. 已新增颜色/alpha track 的 full survey 聚合字段：`ColorTrack*`、`ColorTrackTypeCounts`、`ColorTrackInitialMatchTypeCounts`、`AlphaOpacity*`。重新生成 JP/EN full survey 后，颜色 track JP/EN 为 5,726/5,774 条、key 为 11,157/11,231 个，所有 key 均在 `0..1`，所有目标初始通道 present；初始通道匹配为 5,371/5,437。type 24 JP/EN 为 9,849/9,977 条、key 为 19,242/19,427 个，所有 key 均在 `0..1`，所有目标 material alpha present；初始 alpha 匹配为 8,142/8,229，CIMG targets 为 4,675/4,701，display=false targets 为 2,153/2,173。该结果只确认通道关联和值域，不确认混合/初始化规则。
54. 已新增 `CIMG.0x48` bit 的 full survey 交叉聚合字段：`CimgFlagBitDisplayFalseCounts`、`CimgFlagBitMultiReferenceCounts`、`CimgFlagBitSecondaryReferenceCounts`、`CimgFlagBitNonZeroReferenceIndexCounts`、`CimgFlagBitMissingNodeCounts`、`CimgFlagBitNodeFlagCounts`、`CimgFlagBitGroupCounts`、`CimgFlagBitPairCounts`。重新生成 JP/EN full survey 后，`0+22` 与 `20+23` 强共现但不命名语义；bit 15 高覆盖但不是全局必有；非零 `0x45` group index 与高位 bit 只少量交叉；所有 CIMG bit 记录均能绑定目标 NODE。
55. 已新增 `NODE.0x30` bit 的 full survey 节点级交叉聚合字段：`NodeFlagBitDisplayFalseNodeCounts`、`NodeFlagBitCimgTargetNodeCounts`、`NodeFlagBitAnimatedNodeCounts`、`NodeFlagBitDataNodeCounts`、`NodeFlagBitCategoryRecordNodeCounts`、`NodeFlagBitCategoryNonZeroNodeCounts`、`NodeFlagBitExactFlagCounts`、`NodeFlagBitGroupCounts`、`NodeFlagBitImageCastFlagBitCounts`、`NodeFlagBitTrackTypeCounts`、`NodeFlagBitPairCounts`。重新生成 JP/EN full survey 后，bit0 CIMG target node 为 7063/7214，bit11 为 7061/7214，bit16 为 20/26；`0x701` 两个节点与 `0x700` 一个节点说明 bit11 不能写成全局必有。字段名明确为 node count，避免和 image cast record count 混淆。
56. 已新增 `TRK.0x54 extra mask` 的 full survey 交叉聚合字段：`TrackFlagExtraSceneCounts`、`TrackFlagExtraBaseCounts`、`TrackFlagExtraAnimationCounts`、`TrackFlagExtraTrackTypeCounts`、`TrackFlagExtraKeyValueTypeCounts`、`TrackFlagExtraNodeFlagCounts`、`TrackFlagExtraNodeFlagBitCounts`、`TrackFlagExtraGroupCounts`、`TrackFlagExtraCimgTargetCounts`、`TrackFlagExtraInitialDisplayCounts`、`TrackFlagExtraCimgFlagCounts`、`TrackFlagExtraCimgFlagBitCounts`、`TrackFlagExtraCimgReferenceCountCounts`。重新生成 JP/EN full survey 后，`0x100` 为 3063/3066 条、出现在 123/124 个场景；CIMG target 为 1818/1821，非 CIMG target 为 1245/1245，初始 display=false 仅 214/214。该结果推翻“全为 CIMG/action special”的全局表述；当前只保留 raw extra mask，不命名运行时语义。
57. 已新增 `KEY.0x5C/0x5D/0x5E` 的 full survey 交叉聚合字段：`KeyInterpolationCounts`、`KeyInterpolationTrackTypeCounts`、`KeyInterpolationKeyValueTypeCounts`、`KeyTangentPresentInterpolationCounts`、`KeyTangentPresentTrackTypeCounts`、`KeyTangentNonZeroInterpolationCounts`、`KeyTangentNonZeroTrackTypeCounts`、`KeyTangentMismatchInterpolationCounts`、`KeyTangentMismatchTrackTypeCounts`、`KeyTangentMismatchAnimationCounts`、`KeyTangentMismatchNodeFlagCounts`、`KeyTangentMismatchGroupCounts`、`KeyTangentMismatchTrackExtraCounts`、`KeyTangentMismatchTangentPairCounts`，并在 aggregate 中补充 `KeyTangentPresent`、`KeyTangentNonZero`、`KeyTangentNonZeroScenes`、`KeyTangentMismatchScenes`。重新生成 JP/EN full survey 后，`0x5C` 只见 `0/1/2`；`0x5D != 0x5E` 为 123/123 个 key、9/9 个场景，覆盖 RotateZ/TranslateY/ScaleX/ScaleY，推翻“只在 Otohime RotateZ”的全局表述。
58. 已新增 `NCAT` detail 的 full survey 交叉聚合字段：`NcatKindTypeByteCounts`、`NcatKindCategoryCounts`、`NcatTypeByteCategoryCounts`、`NcatKindParameterPresenceCounts`、`NcatParameterStringCounts`、`NcatKindNodeFlagCounts`、`NcatKindNodeFlagBitCounts`、`NcatKindNodeGroupCounts`、`NcatKindDisplayCounts`、`NcatKindCimgTargetCounts`、`NcatKindAnimatedNodeCounts`。重新生成 JP/EN full survey 后，`(none)` 全部为 type `?` + category `0`，`extparamdata/fontparamdata/mkrubytextdata` 主要为 type `0x9`，`fontslot/fontnlo` 主要为 type `0x5`；当时的 string-only `NcatParameterStringCounts` 只显示 6 类字符串模板。后续已复核 `NCAT.0x0F` 实际为 `String/RawByte03/Int32/Float32` 混合 payload，因此该结果只作为字符串子集与 NODE 同 index 绑定的分布证据，不命名 kind/type/category 或参数 payload 的运行时功能。
59. 已新增 compact tail raw/cross full-survey 聚合字段，覆盖 `CNUM.0x48/0xA0/0xA1`、`TEXT.0x7A/0x41/0x78/0x79/0x7C`、`CSLI.0x40..0x43/0x80..0x87/target node context`、`SLIC.0x40/0x41/0x45/0x37/0x38/0x39` 与 `SLIC.0x83` 交叉。重新生成 JP/EN full survey 后，`CNUM.0x48=32768` 为 217/217，`CNUM.0xA1|0x44` 与 `0xA1|CREF record count` 聚合一致；`CSLI` target node 均为 `NODE.0x30=0xF02/display=true/non-CIMG target`；`SLIC.0x37=#FFFFFFFF`、`0x38=#FF000000`、每条 `0x39` 为 4 个 Color32。后续补强已把这些 `CSLI` 字段和 `SLIC` Color32 raw hex 带入只读模型/JSON/Markdown/inspect。所有这些都保持 raw/unknown semantics，不命名为数字控件、文本布局、九宫格切片或颜色通道运行时功能。
60. 已新增 `CNUM.0xA1` 与 `TEXT.0x7A` 的 raw string byte-shape survey 字段：`RawLengthCounts`、`ContentLengthCounts`、`Utf8StatusCounts`、`ShiftJisByteShapeCounts`、`RawPreviewCounts`。重新生成 JP/EN full survey 后，`CNUM.0xA1` 为 217/217 ASCII-only 且 strict UTF-8 valid；`TEXT.0x7A` 为 JP 97/105、EN 123/131 strict UTF-8 invalid，这些 invalid 项全部符合 Shift-JIS byte-shape with non-ASCII，另有 8/8 ASCII-only。该结果只确认 raw byte 形态和编码候选，不把 `TEXT.0x7A` 写成已确认 Shift-JIS 渲染文本。
61. 已新增 AVTS header unknown word 的 full-survey offset 级聚合字段：`HeaderUnknownNonZeroOffsetCounts`、`HeaderUnknownWordValueCounts`、`HeaderUnknownWordOffsetValueCounts`、`HeaderUnknownWordOffsetClassCounts`。重新生成 JP/EN full survey 后，SVO 仍为 47/47 与 47/47 全解析、YABX unparsed bytes 为 0、directory reserved 非零为 0。offset/value 聚合显示没有任何 unknown word offset 有全覆盖非零常量；只有 `0x70=0` 在两组各 47/47 稳定。`0x6C` 的 class 在两组各 47/47 都是 in-file-offset-candidate，但 raw value 变化，不能命名为固定 offset 字段。
62. 已新增 VTBF tag 参数和 full-survey 字段目录聚合字段：`VtbfTagCounts`、`VtbfTagParamRawCounts`、`VtbfTagParamLowHighCounts`、`VtbfTagParamHighPropertyCountCounts`、`VtbfTagTrailingByteCounts`、`VtbfKeyParamHighModulo5Counts`、`VtbfFieldDirectoryCounts`、`VtbfFieldDirectoryBlockCounts`、`VtbfFieldCountValueCounts`、`VtbfFieldStrideValueCounts`。重新生成 JP/EN full survey 后，observed tag 为 26 类，字段目录为 158 类，未知 type code 为空，所有块 trailing bytes 为 0；`KEY.ParamHigh % 5` 全为 0，`KEY.ParamHigh` 与实际字段数无 mismatch；`TRK` 全为 `ParamLow=1 / ParamHigh=5 / Fields=5`。同时修正文档中 bit15、Shift-JIS 长注释、AVTS header 和 SvoToolOutput 的边界表述，避免把候选写成正式语义。
63. 已新增 compact tail record shape profile：`CnumFieldSequenceCounts`、`CnumFieldSetCounts`、`TextFieldSequenceCounts`、`TextFieldSetCounts`、`SliceCastFieldSequenceCounts`、`SliceCastFieldSetCounts`、`SliceRecordFieldSequenceCounts`、`SliceRecordFieldSetCounts`、`SliceRecordShapeCounts`。重新生成 JP/EN full survey 后，`CNUM` 217/217 只有 4 种字段顺序且差异仅为 zero marker id；`TEXT` 105/131、`CSLI` 26/26、`SLIC` record 108/108 都只有 1 种字段顺序。`SLIC` shape tuple 为 45 种，每条 record 均有 4 个 `0x39` Color32；输出层现在同时显示这些 Color32 的 `#AARRGGBB` 与 raw hex。这些只作为 raw record layout，不命名运行时语义。
64. 已新增 TRK/KEY 结构一致性 survey：`TrackKeyStorageMatrixCounts`、`TrackFieldSequenceCounts`、`KeyFieldSequenceCounts`、`TrackFrameRangeRelationCounts`、`TrackKeyFrameOrderCounts`、`TrackKeyFrameDuplicateCounts`、`TrackFirstFrameDeltaCounts`、`TrackLastFrameDeltaCounts`。重新生成 JP/EN full survey 后，94,611/95,558 条 TRK 只有一种字段顺序 `0x53,0x57,0x54,0x58,0x59`；186,780/188,002 个 KEY 只有 4 种字段顺序，差异只在 `0x5B` type。全部 track 的 key frame 为非递减，first frame 均等于最小 key frame；last frame 等于最大 key frame 为 91,185/92,114，last frame 大于最大 key frame 为 3,426/3,444。重复 frame 仅 2/2 条，定位到 `MM_UI_DetailOption__Base_UI.sbscene` 与 `MM_UI_Shiborikomi__Base_UI.sbscene` 的 `FadeIn -> cover` type 24 alpha 轨道，frame 4 有两个不同 alpha key。该结果只确认结构边界和序列关系，不命名重复 frame 覆盖规则、播放 hold 规则或具体插值算法。
65. 已新增 transform track full-survey 聚合字段：`TransformTrackCount`、`TransformTrackKeyCount`、`TransformTrackTypeCounts`、`TransformTrackStorageCounts`、`TransformTrackKeyValueKindCounts`、`TransformTrackInitialMatchTypeCounts`、`TransformTrackValueRangeCounts`、`TransformCandidateDefaultKeyCounts`。同时补齐 `AnimationMotionStructureSurvey`/`SceneSurveyAggregate` 输出模型，使现有 animation/motion 结构聚合重新参与 JSON 输出。重新生成 JP/EN full survey 后，transform track JP/EN 为 68,482/69,174 条、key 为 124,712/125,579 个；key missing value 为 0/0。type `0/1/5/6/7` 绑定目标 `TRS2` 初始通道，至少一个 key 匹配为 64,375/65,001，mismatch 为 3,543/3,609；type `2/3/4/8` 无可比初始通道，但全部 key 都等于候选默认值 `0/0/0/1`。该结果只确认 transform track 的存储、值域和初始通道交叉关系，不确认 Z 轴运行时规则、初始化规则或角度 wrap/clamp。
66. 已核对当前 `plan.md` 的剩余后续动作：现有 surfboard/surfboard_EN full survey 能静态确认的 transform 结构数据已同步到 JSON、文档和本计划；剩余 `unknown/candidate` 项均需要更多版本样本、运行时代码或 writer 设计才能继续提升置信度。当前不将运行时混合规则、初始化规则、3D 轴语义、bit 精确语义、hash/checksum 或资源选择规则写成正式数据。
67. 已将 `CROP.0x65` / `CREF.0x49` compact record 的 raw bytes 保留为 `RawHex`，并新增 full-owner CREF survey 字段：`CropRectCount`、`CropAtlasDeclaredCount*`、`CropRectInAtlasBounds`、`CropRectOutOfAtlasBounds`、`CropReferenceCount`、`CropReferenceKindCounts`、`CropReferenceOwnerCounts`、`CropReferenceTextureListIndexCounts`、`CropReferenceTextureIndexRangeCounts`、`CropReferenceCropIndexRangeCounts`。重新生成 JP/EN full survey 后，CREF selector 计数从旧的 CIMG-only `17,069/17,206` 修正为全 owner `19,597/19,734`；CNUM/CSLI 分别贡献 `2,422/106` 条。运行时代码已确认 selector 不是业务 kind：`CREF.0x49` selector `1` 选择 3 个 int16 tuple，`CROP.0x65` selector `2` 选择 4 个 int16 crop rect。该结果不命名 JP `65535` 或越界 crop 的运行时语义。
68. 已新增 CROP/CREF 异常样本定位字段：`CropRectOutOfAtlasBoundsReasonCounts`、`CropReferenceOutOfRangeOwnerCounts`、`CropRectOutOfAtlasBoundsSamples`、`CropReferenceOutOfRangeSamples`。重新生成 JP/EN full survey 后，CROP 越界原因分布为 JP `right>width=104, left<0=51, right>width+bottom>height=46, left<0+top<0+right>width+bottom>height=34, top<0=15, bottom>height=6, top<0+right>width+bottom>height=3`，EN `right>width=83, right>width+bottom>height=47, left<0+top<0+right>width+bottom>height=36, top<0=15, bottom>height=6, top<0+right>width+bottom>height=3, left<0=1`。JP 的 2 条 CREF out-of-range 均为 CIMG owner，分别位于 `MM_UI_Common__MM_UI_Common_Ranking_ADV.sbscene` 的 `ranking_base` 和 `MM_UI_SimpleOption__Reference_TitleFrame_Small_ALL.sbscene` 的 `hard_text`，raw 均为 `01FFFFFFFFFFFF`；EN 无 CREF out-of-range。该结果只作为 raw sample 定位，不解释 sentinel/空引用或运行时裁剪。
69. 已新增 `CIMG.0x45` raw index 分布和非零样本定位字段：`Cimg45GroupIndexCounts`、`Cimg45GroupCountIndexCounts`、`Cimg45NonZeroGroupCounts`、`Cimg45NonZeroSamples`。重新生成 JP/EN full survey 后，非空 CREF group 仍全部 in-range（JP 7315/7315，EN 7454/7454），out-of-range 与 empty group non-zero 均为 0。JP 非零 group 为 131（primary 127、secondary 4），EN 为 124（primary 120、secondary 4）；JP 独有一个 `primary|70|64`，定位到 `MM_UI_ButtonCounter__MM_UI_Common_Switch.sbscene` 的 `text`，indexed raw 为 `01000000001D00`。secondary 非零 4/4 定位到 `MM_UI_MusicSelectFinaleCourseAccept` 的 `BG_Black1/BG_Black2`，index 均为 1。运行时代码已确认它们是 primary/secondary 两组 CREF 的组内 index；默认/当前/选中引用的高层命名仍待确认。
70. 已复核 `AnimationMotionStructureSurvey` 的 ANIM/MOT 结构聚合并修正文档边界：JP/EN 的 `ANIM` 字段顺序只有一种，覆盖 1,579/1,614 个动画；`ANIM.ParamLow` 与实际 motion 数匹配 1,167/1,170，另有 412/444 个为 `motion count + 1`；`ANIM.0x50` 与实际 motion 数匹配 1,578/1,613，唯一异常为 `MM_UI_Entry__MM_UI_Select_EntryName_ALL.sbscene` 的 `NameFadeIn`（`ParamLow=4`、`0x50=6`、实际 motion 数 4）。后续复核该异常原始 dump 可见 4 个 `MOT` 的 track 数为 `6,2,6,2`；新增 `AnimationField50MotionOrMaxTrackRelationCounts` 后，JP/EN 均只有这个异常为 `equalsMaxMotionTrackCountOnly`，其它为 `equalsMotionCountOnly` 1255/1280、`equalsMotionCountAndMaxMotionTrackCount` 278/287、`noMotions` 45/46。因此 `ANIM.0x50` 也不能改名为 max track count。`MOT.ParamLow`、`MOT.0x52` 与实际 track 数在 32,428/32,742 个 motion 中全量一致。当前只确认这些结构分布，不把 `ANIM.ParamLow` 或 `ANIM.0x50` 写成全局 motion count。
71. 已确认 `D:\maimai FiNALE (SDEY 1.99.00)\maimai\data` 下当前只有 `surfboard` 与 `surfboard_EN` 两个目录包含 `.sbscene/.svo`，计数分别为 314/47 与 318/47。现有本地样本范围内 full survey 仍为 scene 314/314、318/318 全解析，SVO 47/47、47/47 全解析，unknown VTBF type code 为空，YABX unparsed bytes 为 0。后续若要把 bit 语义、运行时混合、hash/checksum 或 writer 规则提升为正式数据，需要引入新的可验证证据源。
72. 本轮重新执行 `dotnet test .\SbScene.sln --no-restore`（20/20 通过）、`dotnet build .\SbScene.sln --no-restore`（0 warning / 0 error），并重新生成 JP/EN full survey。复核结果：scene 314/314 与 318/318 全解析，`ScenesWithWarnings=0`，`UnknownTypeCodeCounts={}`，`DataBlocksWithFields=0`，`DataBlocksWithTrailingBytes=0`；SVO 47/47 与 47/47 全解析，`YabxWithUnparsedBytes=0`、`YabxUnparsedBytes=0`、directory reserved 非零 entries/bytes 均为 0。JP/EN 的 observed tag key 集合一致，`VtbfFieldDirectoryCounts`、`VtbfFieldDirectoryBlockCounts`、`VtbfFieldCountValueCounts`、`VtbfFieldStrideValueCounts` key 集合也完全一致；当前没有新的静态结构项可从现有样本继续提升为正式语义。
73. 2026-05-29 再次执行 `dotnet test .\SbScene.sln --no-restore`（20/20 通过）、`dotnet build .\SbScene.sln --no-restore`（0 warning / 0 error），并用当前 CLI 重新生成 `out/survey-surfboard-all.json` 与 `out/survey-surfboard-en-all.json`。复核结果仍为 JP scene 314/314、EN scene 318/318、warnings 0、unknown type code 0、VTBF field directory 158/158；SVO 47/47 与 47/47 全解析，YABX unparsed bytes 0，directory reserved non-zero bytes 0。已在本计划顶部补充“完成口径”和 VTBF 字段目录 tag 级完成快照，明确正式数据只限可复算 raw 结构，候选/unknown 不作为协议事实。
74. 2026-05-29 最终收口复核：重新执行 `dotnet test .\SbScene.sln --no-restore`（20/20 通过）、`dotnet build .\SbScene.sln --no-restore`（0 warning / 0 error），并重新生成 JP/EN full survey。大小写敏感 JSON 复核确认：JP scene 314/314、EN scene 318/318 均无 parse error 与 warning；`UnknownTypeCodeCounts={}`，`DataBlocksWithFields=0`，`DataBlocksWithTrailingBytes=0`；两组 `VtbfTagCounts` 均为 26 类，`VtbfFieldDirectoryCounts` / `VtbfFieldDirectoryBlockCounts` / `VtbfFieldStrideValueCounts` 均为 158 类，`VtbfFieldCountValueCounts` 均为 509 类，且 JP/EN key 集合完全一致；SVO 两组均为 `Total=47 / Parsed=47 / Failed=0`，`YabxWithUnparsedBytes=0`、`YabxUnparsedBytes=0`、directory reserved non-zero entries/bytes 均为 0，YABX declared payload length 47/47 匹配。当前本地样本可静态验证的数据已完成；剩余项目继续只作为 `unknown` / `candidate`，不提升为正式数据。
75. 2026-05-29 运行时代码复核 `CIMG.0x48`：`sub_88EC90` 将 `0x48` 保存为结构首 dword 后拆成多个 enum/bool；同一组 decoder 也由 `CSLI.0x80`、`CNUM.0x48`、`TEX.0x62`、`LAYR.0x20`、`SLIC.0x83` 等路径复用。已把文档和输出文案从 CIMG 私有 bit 语义收窄为 shared packed state word 与已知 mask 布局；业务语义仍待渲染状态消费链确认。
76. 2026-05-29 继续复核 shared packed state decoder xref：`0xF0/0xF00` helpers 覆盖 `TEX` 与 image/slice/number cast；低位 `0x10/0x20/0xC0` helpers 覆盖 `CIMG` 与 `SLIC`；`0xF00000/0x01000000/0x02000000` 目前只见 `CIMG` 装载路径调用。尝试从 node resource slot / CIMG struct offset 做 IDA 全库 operand scan 时 IDA 主线程超时，未能取得可提升业务语义的新 consumer 证据；文档继续保持 raw packed state / runtime bitfield layout 口径。
77. 2026-05-29 为 `CNUM.0xA1` 与 `TEXT.0x7A` 补齐逐记录 raw byte 证据：只读模型新增 `FieldA1RawHex` / `Field7ARawHex`，JSON camelCase 输出为 `fieldA1RawHex` / `field7ARawHex`，Markdown 与 inspect 同步展示 raw hex 分布和样本列。现有 `FieldA1` / `Field7A` 宽松字符串显示保持不变。重新执行 `dotnet test .\SbScene.sln --no-restore`（20/20 通过）和 `dotnet build .\SbScene.sln --no-restore`（0 warning / 0 error）。该改动只增强 `TEXT.0x7A` 编码候选的 raw 证据可复算性，不把它提升为已确认 Shift-JIS 文本。
78. 2026-05-29 补齐 `TEXT.0x7B` 结构化暴露：`SbSceneTextRecord` 新增 `Field7BRawHex` / `Field7BPackedValues`，JSON camelCase 输出为 `field7BRawHex` / `field7BPackedValues`，Markdown 与 inspect 同步展示 `0x7B` raw hex 和 packed value 分布。`TEXT` 单元测试改为真实 `0x7B type=0x45` record shape，并新增 JSON 输出断言。重新执行 `dotnet test .\SbScene.sln --no-restore`（21/21 通过）。该改动只确认 `0x7B` 字段边界与低层 packed record 机械解码，不命名布局或渲染业务语义。
79. 2026-05-29 继续尝试窄范围 IDA 验证 `TEXT`/packed state：只查询 `TEXT` tag、已知函数名与少量 immediate，但当前 IDA 会话对 `find`/`lookup_funcs` 均 120 秒超时，未获得新的稳定逆向证据。因此本轮不更新 `TEXT.0x7A/0x7B` 或 `CIMG.0x48` 的运行时语义口径。
80. 2026-05-29 继续审计文档模糊项后，围绕 `ANIM.0x50` 唯一 `+2` 异常新增 `AnimationField50MaxMotionTrackDeltaCounts` 与 `AnimationField50MotionOrMaxTrackRelationCounts`。重新生成 JP/EN full survey 后，`0x50` 与 actual motion count 的异常仍只有 `MM_UI_Entry__MM_UI_Select_EntryName_ALL.sbscene` / `NameFadeIn`；原始 dump 显示其 4 个 `MOT` track 数为 `6,2,6,2`，所以 `0x50=6` 只是该异常里等于最大 motion track 数。全量关系为 JP `equalsMotionCountOnly=1255, equalsMotionCountAndMaxMotionTrackCount=278, equalsMaxMotionTrackCountOnly=1, noMotions=45`，EN `1280/287/1/46`，因此 `ANIM.0x50` 不能提升为 confirmed motion count，也不能改名为 max track count。
80. 2026-05-29 补齐 `TEXT.0x33` 结构化暴露：`SbSceneTextRecord` 新增 `Field33RawHex` / `Field33Vector`，JSON camelCase 输出为 `field33RawHex` / `field33Vector`，Markdown 与 inspect 同步展示 `0x33` raw hex 和 vector 分布。`TEXT` 单元测试使用非零 vector 样本并断言模型与 JSON 输出。重新执行 `dotnet test .\SbScene.sln --no-restore`（21/21 通过）。该改动只确认 `0x33 type=0x4A` 字段边界与低层 VectorFloat32 机械解码，不命名布局、换行或渲染语义。
81. 2026-05-29 补齐 `CNUM` record shape 的高层结构化暴露：`SbSceneCnumRecord` 现逐记录暴露 `Field40/42/43`、`Field39Colors` / `Field39RawHexValues`、`FieldA2..FieldAD`、`FieldAERawHex` / `FieldAEFloatValues`、`FieldAFRawHex` / `FieldAFPackedValues`，Markdown 与 inspect 同步展示分布和样本列。合成 `CNUM` 单元测试覆盖真实 marker-before-`0xA1` shape、Color32 raw、`0xAE type=0x4A`、`0xAF type=0x45` 以及 JSON camelCase 输出。重新执行 `dotnet build .\SbScene.sln --no-restore`（0 warning / 0 error）和 `dotnet test .\SbScene.sln --no-restore --no-build`（21/21 通过）。该改动只确认字段边界与低层机械解码，不命名数字控件、布局或颜色运行时语义。
82. 2026-05-29 为 `TEXT.0x7A` 增加严格 CP932/Shift-JIS 解码 survey 字段 `TextField7AShiftJisDecodeStatusCounts`，CLI 项目显式引用并注册 `System.Text.Encoding.CodePages`。重新生成 JP/EN full survey 后，JP 为 `validShiftJis=105/105`，EN 为 `validShiftJis=131/131`；构建 0 warning / 0 error，测试 21/21 通过。该结果把 `TEXT.0x7A` 从单纯 byte-shape candidate 提升为 raw content 可严格解码证据，但仍不确认运行时最终文本编码、布局或渲染语义。
82. 2026-05-29 补齐 `CSLI/SLIC` 与 `CRFD` 输出层 raw 证据：`SbSceneSliceRecord` 新增 `Field37RawHex` / `Field38RawHex` / `Field39RawHexValues`，Markdown 与 inspect 同步展示 `CSLI.0x40..0x43/0x80..0x87`、`SLIC.0x40/0x41/0x45`、`SLIC.0x37/0x38/0x39` 的 Color32 与 raw hex 分布；`SbSceneCrfdRecord` 新增 `Field90RawHex` / `Field91RawHex`，Markdown 与 inspect 同步展示 `CRFD.0x94` 分布。合成 `CSLI/SLIC` 与 `CRFD` 测试已补 JSON camelCase 断言。重新执行 `dotnet build .\SbScene.sln --no-restore`（0 warning / 0 error）和 `dotnet test .\SbScene.sln --no-restore`（21/21 通过）。这些改动只确认 raw bytes 与低层机械解码，不命名 slice、source resource 或颜色运行时语义。
83. 2026-05-29 针对文档中 `type 18/19` image variant 分组仍模糊的问题，新增 `ImageVariantGroup*` full-survey 聚合字段，保留旧 `ImageVariant*` 的 type 18 primary+secondary 合计宽松检查，同时按运行时 validator 分组验证 `18 primary` 与 `19 secondary`。重新生成 JP/EN full survey 后，`18 primary` 为 1,791/1,792 条 track、6,986/6,988 个 key，全部有 CIMG 且全部 in range；`19 secondary` 两组均为 333 条 track、9,769 个 key，也全部有 CIMG 且全部 in range。`19 secondary` CREF count 分布为 `2:41, 5:16, 32:276`，key value 分布为 0..31。已同步更新 `docs/animation-system.md`、`docs/sbscene-format.md`、`docs/svo-resources.md`；重新执行 `dotnet build .\SbScene.sln --no-restore`（0 warning / 0 error）和 `dotnet test .\SbScene.sln --no-restore --no-build`（21/21 通过）。该改动确认 primary/secondary CREF 组范围关系，不确认播放时与 `CIMG.0x45` 的默认/当前/选中组合语义。
84. 2026-05-29 复核 YABX descriptor label 时发现旧输出 `InlineOrString/StringOrInline` 会误导为字符串/inline 容器，但 full survey 已证明 raw `000000` 与 `010000` 都同时承载 `ReferenceList` 和 `String`。解析器现将 `valueKind=0x00` 统一标为 `OwnerDependent`，并在 `docs/svo-resources.md` 与本计划中明确它只是排查标签，实际 payload 容器仍由 owning type / field 顺序决定。IDA 当前会话对最小 `py_eval` 仍 120 秒超时，本轮未获得新的运行时代码证据。

85. 2026-05-29 已用当前 CLI 刷新 Ras/Chiffon/Otohime 的 `*-svo-inspect.txt` 与 `*-svo-extract/manifest.json`，并同步刷新 gage、UI Background、UI Announce、UI ResultInfo、UI Aime 的旧 inspect / extract 生成物。复核确认 `out/` 下现有 `*-svo-inspect.txt` 与 `manifest.json` 不再输出 `InlineOrString/StringOrInline`，只保留 `OwnerDependent/Int32Like/ObjectReference` 等当前候选标签；旧标签仅留在本计划的历史说明中，用于解释本次重命名原因。
86. 2026-05-29 针对 `CRFD.0x90/0x91` 仅有单字段分布、来源场景/资源名候选证据偏弱的问题，新增 `CrfdField90Field91Counts` 与 `CrfdField90Field91Field92Counts` full-survey 聚合字段。重新生成 JP/EN full survey 后，JP/EN 均为 94 个 `0x90|0x91` 组合、每个组合只对应一个 `0x92` 值；唯一 JP/EN 计数差异为 `MM_UI_NameEntry|Reference_Player_Name_Parts|2` 的 8/16。该结果确认 `0x90/0x91/0x92` 的稳定组合关系，但仍不命名 `0x90/0x91` 的运行时解析语义，也不命名 `0x92` 的业务含义。
87. 2026-05-29 继续回扫 ANIM 结构字段：将 `AnimationField50MaxMotionTrackDeltaCounts` 的无 motion 分类从不清晰的 `?` 改为 `noMotions`，重新生成 JP/EN full survey 后对应顶层计数为 45/46，测试 21/21 通过。同步复核 `ANIM.0x56`：相对最大 `TRK.0x59` 的关系为 JP/EN `endEqualsMaxTrackLast=1396/1428`、`endContainsMaxTrackLast=91/93`、`endBeforeMaxTrackLast=47/47`、`noTrackLastFrames=45/46`；相对最大 `KEY.0x5A` 的 delta 为 `0=1394/1426`、negative total `47/47`、positive total `93/95`、`noKeyFrames=45/46`。Shama 原始 dump 中多个 `Action_*` 的 `0x56=150/200`，但 track/key 最大帧可延伸到 `154..227`，多个 `Mouth_*` 则停在 `146`。因此 `0x56` 只能写成 declared/nominal end frame candidate，不能写成所有 track/key 的严格最大帧或裁剪边界。

87. 2026-05-29 针对 `CIMG.0x45` 与 type 18/19 image variant 起始 key 的关系补齐文档边界。现有 `ImageVariantGroupCimg45FirstKey*` full-survey 字段显示：JP primary 1533 match / 258 mismatch、secondary 332 match / 1 mismatch；EN primary 1534 match / 257 mismatch / 1 multi-CIMG target、secondary 332 match / 1 mismatch。已同步更新 `docs/animation-system.md`、`docs/sbscene-format.md`、`docs/svo-resources.md`、`docs/ras-sample.md` 与本计划，统一写为 `0x45` 是 primary/secondary CREF 组内静态 index，且经常与最早 key 对齐但非全覆盖；不命名为动画初始 key、默认引用、当前引用或选中引用。重新执行 `dotnet build .\SbScene.sln --no-restore`（0 warning / 0 error）和 `dotnet test .\SbScene.sln --no-restore --no-build`（21/21 通过）。
88. 2026-05-29 清理 type 19 输出命名残留：解析器现将 type 18/19 分别命名为 `PrimaryImageVariantIndexCandidate` / `SecondaryImageVariantIndexCandidate`，不再让 type 19 回落为 `TrackType19`。Markdown/inspect 的状态轨道摘要也已纳入 type 19，并把 `ImageRefCheck` 从合计 CREF 范围改为按 type 18 primary、type 19 secondary 分组检查；相关文档同步说明 legacy `ImageVariant*` 字段仅为早期 type 18 合计宽松统计。
89. 2026-05-29 补齐 `ANIM.0x5F` 文档空白并新增 full-survey 交叉字段：`AnimationField5FCounts`、`AnimationField5FMotionPresenceCounts`、`AnimationField5FAnimationNameCounts`、`AnimationField5FParamLowMotionDeltaCounts`、`AnimationField5FField50MotionDeltaCounts`、`AnimationField5FField50RelationCounts`、`AnimationField5FEndFrameRelationCounts`。重新生成 JP/EN full survey 后，`0x5F` 只见 `0/1`，分布为 JP `0=1438, 1=141`、EN `0=1469, 1=145`；`0x5F=1` 中有 motion 为 136/140、无 motion 为 5/5，因此不是 motion-presence flag。`0x5F=1` 时 `0x50` 全部等于实际 motion 数，但 `ANIM.ParamLow` 仍同时有 `motion count` 与 `motion count + 1` 两类；`0x56` 关系也覆盖等于、包含、短于最大 track last 和无 track。名称上 `0x5F=1` 多数为 loop 类（127/131），但有 `Action`、`red_ef`、`life_*` 等反例。文档现仅写为 Byte/Bool-like raw flag，不命名 loop/playback/active/count modifier。`dotnet build .\SbScene.sln --no-restore` 与 `dotnet test .\SbScene.sln --no-restore --no-build` 均通过（21/21）。
90. 2026-05-29 继续审计 `type 28` 与 `TRK.flags` 旧口径：用当前 JP/EN full survey 复核，type 28 均为 29 条 track、31 个 key，全部目标有 `TRS2.0x38` illumination alpha 初始通道，且 29/29 条 track 至少一个 key 匹配初始 alpha；文档和计划已从“仍需更多样本”的空泛表述收紧为具体通道匹配证据，但仍只保留 `IlluminationAlphaCandidate`，不命名运行时渲染 alpha 规则。`TRK.flags` 文档同时明确 storage nibble 指 `base byte = flags & 0xFF` 的 high nibble，不再让读者把 `flags >> 4` 当作带 extra mask 时的整体 storage。
91. 2026-05-29 继续回扫生成器术语残留：`MarkdownExporter` 的 CIMG 值域校验标题和表格说明不再把 `CIMG.0x45` 写成宽泛的 `reference index candidate`，改为 primary/secondary CREF 组内 raw/stored index 口径，并重新生成 `out/` 下 17 个现有 Markdown 报告以清掉旧文本。该改动只修正报告文本，与既有结论一致：`0x45` 是组内静态 index，但仍不命名为默认、当前、选中或动画起始 key。复扫确认 `src/docs/out` 中旧短语已清零；`dotnet build .\SbScene.sln --no-restore` 为 0 warning / 0 error，`dotnet test .\SbScene.sln --no-restore --no-build` 为 21/21 通过。
92. 2026-05-29 继续审计 SVO/YABX reference base 表述：复核 `SvoResourceParser.InferObjectReferenceBase` 的静态反查逻辑与现有 full survey 聚合，JP/EN 两组 `YabxReferenceBaseCounts` 均为 `0x2711:47`，`YabxObjectCountDdsSkeletonMatches` / `YabxObjectTypeOrderDdsSkeletonMatches` 均为 47/47，`Texture._image` 到 paired Image reference 为 281/281 + 281/281 match 且无 mismatch/missing。`docs/svo-resources.md` 已从“通用规则仍需更多样本确认 / candidate”收紧为：当前 `Database / VertexDeclaration / VertexElement / Texture / Image` resource skeleton 内可按 `referenceId - 0x2711` 做静态 object index 反查；未知 YABX 版本或新增对象骨架是否固定同一 base 仍不外推。
93. 2026-05-29 继续清理 `docs/ras-sample.md` 的旧三样本/单样本措辞：`TranslateZCandidate` 不再写“轴向命名仍待更多样本”，改为 Ras 与 full survey 均只确认默认值 0 且没有可比 `TRS2` 初始通道；YABX 限制项不再停留在 Ras 14/14，而是写明 full survey 当前 resource skeleton 已字段级完整覆盖，待确认范围只剩其它 YABX 版本或新增对象类型。
94. 2026-05-29 继续收紧剩余“更多样本”措辞：`docs/svo-resources.md` 的 YABX payload 泛化待办改为“当前 resource skeleton 内已覆盖，其他 YABX 版本/新增对象类型另行验证”；`docs/sbscene-format.md` 的 `NODE.0x30` bit 语义改为需要运行时用途证据或更多 flags pattern，而不是泛泛写更多样本。现有 full survey 已给出 observed bits、bit0/CIMG、bit11 例外和 bit1/2 分布，仍不提升 bit 业务语义。
95. 2026-05-29 复核 `MOT.0x51` 的旧 Ras-only 表述：现有 survey 聚合 `MotionTargetIndexRangeCounts` 为 JP `inRange=32428`、EN `inRange=32742`，`MotionField52TrackDeltaCounts` 与 `MotionParamLowTrackDeltaCounts` 均只有 `0`。`docs/animation-system.md` 已把 `MOT.0x51` 从 Ras 样本范围证据收紧为 full-survey NODE index 覆盖；运行时 target 角色仍保持候选绑定，不提升业务语义。
96. 2026-05-29 继续清理 `docs/svo-resources.md` 开头的 reference base 旧口径：最早三样本表现在明确标为 checkpoint，后文证据链改为 MM_CH 7/7 与 surfboard/surfboard_EN 47/47 + 47/47 full survey；当前 `Database / VertexDeclaration / VertexElement / Texture / Image` resource skeleton 内可按 `referenceId - 0x2711` 静态反查对象 index。descriptor raw/payload 容器关系仍只提升为 full-survey 观察证据，不命名 descriptor 精确语义。
97. 2026-05-29 继续复核 `docs/sbscene-format.md` 的 `ParamLow/ParamHigh` 主表：用当前 JP/EN full survey 聚合把 `ANIM` 从 Ras-only 口径收紧为 `ParamLow` delta 分布（JP `0=1167, +1=412`；EN `0=1170, +1=444`），并补齐 `TRK`、`KEY`、`CNUM`、`CRFD` 的块数口径。`CNUM|26|27` 与 `TEXT|7|8` 仍明确作为 compact tail 的 raw 结构差异处理，不把 `ParamHigh` 写成所有块的统一字段数规则。
98. 2026-05-29 顺手收紧 `docs/sbscene-format.md` 顶部的结构引导语：`vtc0` 线性 chunk 流与 compact 字段头不再写成 Ras 单样本确认，而是写为当前真实样本解析路径；具体运行时语义仍依各 tag/field 分节确认。
99. 2026-05-29 继续回扫记录块说明：`docs/sbscene-format.md` 中 `SLIC` 记录块不再停留在 UI 样本确认，改为当前 JP/EN full survey 证据（各 26 个 `SLIC` 块、108 条记录、字段顺序唯一、trailing bytes 为 0）。这只确认记录边界和字段顺序，`SLIC.0x83/0x40/0x41/0x45/0x37/0x38/0x39` 的运行时语义仍保持未命名。
100. 2026-05-29 继续回扫 `docs/animation-system.md` 开头字段表：标题不再写成整表仅是 Ras 样本字段边界，改为“以 Ras 样本为起点，full survey 补强项写在说明中”。后续 `TRK.0x57` Ras 分布表和 track type 样例仍作为样本锚点保留，因为文档后文已给出 full-survey 结构聚合。

101. 2026-05-29 继续围绕仍为 unknown 的 `NCAT.0x0E` 做文档收紧：用当前 JP/EN `SceneAggregate` 复核 `NcatCategoryCounts` 与 `NcatKindCategoryCounts`，在 `docs/sbscene-format.md` 补入 category `0..8` 分布和 kind/category 交叉表。该结果只把“已确认分布”具体化：`(none)` 全部为 category `0`，但 `extparamdata/fontparamdata/mkrubytextdata/fontslot/fontnlo` 均可跨多个 category，因此仍不能把 `0x0E` 单独命名为 kind enum 或具体 UI 功能。

102. 2026-05-29 继续收紧 `NODE.0x30` flags 文档：`docs/sbscene-format.md` 不再只列 MM_CH 9 场景最早补充观察到的 `0x800/0xF03/0xF04`，而是按当前 JP/EN `NodeFlagCounts` full survey 列出 28 类 exact flag 的节点数。该表只确认已观察 bit 组合和分布；bit 1/2/8/9/10、bit 16、`0x700/0x701` 等仍需要运行时用途证据才能命名。

103. 2026-05-29 复核 `NODE.0x30` exact flags 表：用 Python 标准库 JSON 读取 `out/survey-surfboard-all.json` 与 `out/survey-surfboard-en-all.json` 的 `SceneAggregate.NodeFlagCounts`，逐项比对 `docs/sbscene-format.md` 中 28 行 full-survey 表。结果无缺项、无额外项、无数值差异；该闭环仅确认 full-survey 分布落文，不提升任何 bit/flag 的运行时语义。

104. 2026-05-29 复扫 `ANIM.0x50/0x56/0x5F`、`TRK.flags`、`CIMG.0x45`、`CRFD`、`NCAT`、`SLIC/TEXT` 等段落，未发现旧的 motion-count/max-track、type 19 或 CIMG index 命名回流。顺手收紧 `docs/ras-sample.md` 的 NCAT 段：Ras/Chiffon/Otohime 全 0 现在明确只作为三角色样本阶段观察，full survey 已扩展出 category `0..8`，但 `0x0E` 仍只保留 raw 分类字段，不提升语义。

105. 2026-05-29 继续复核 `NCAT.0x0F`：`SceneAggregate.VtbfFieldDirectoryCounts` 显示该 field id 在 JP/EN full survey 中不是单纯字符串，分别有 `String` 3852/3959、`RawByte03` 39/39、`Int32` 136/188、`Float32` 61/61。语义层 `ParameterString`/`NcatParameterStringCounts` 只覆盖字符串子集，`ParameterPreview` 与 `Fields` 仍保留非字符串 raw preview；`docs/sbscene-format.md` 已改成混合 payload 参数字段，不确认运行时参数含义。

106. 2026-05-29 反查 `SLIC` 记录 marker：JP/EN full survey 的 26/26 个 `SLIC` 块字段目录只有 `RecordStart` 82/82 与 `RecordEnd` 26/26，没有 `RecordStartFirst`。格式文档已区分 `NODE/TRS2/NCAT` 的 `FC/FE/FD` 记录形态与 `SLIC` 的“首条记录直接从业务字段开始，后续 `FE 00` 分隔，末尾 `FD 00`”形态。

107. 2026-05-29 补齐 `CSLI/SLIC` 块级参数表：`CSLI` 在 JP/EN full survey 中 26/26 块均为 `ParamLow=2 / ParamHigh=13 / fields=13`；`SLIC` 为 `ParamLow=0`，`ParamHigh=33` x21 与 `99` x5，实际 parsed field count 全部匹配。该补充只作为 raw 结构事实，不命名 slice 运行时语义。

108. 2026-05-29 继续追 `PROJ.0x55/0x56` 的 Ras-only 文档残留：用当前解析器的 compact block 布局从 JP/EN 原始 full survey 文件重新抽取 314/318 个 `PROJ` 块。结果显示每个场景 1 个 `PROJ`，字段顺序固定为 `0x0000:0x0006>0x0001:0x0006>0x0005:0x0006>0x0055:0x0008>0x0056:0x0008`，trailing bytes 为 0；`0x55=0` 全覆盖，`0x56` 在两组均有 17 种 raw 值、范围 `50..3000`。相对最大 `TRK.0x59`，JP 为 `less=25, equals=23, greater=247, missing=19`，EN 为 `less=23, equals=20, greater=255, missing=20`；相对最大 `KEY.0x5A` 同样不是严格上界。因此 `docs/sbscene-format.md` 已从旧的 Ras-only frame-range 描述收紧为 scene/project 级 frame end/duration raw 候选，不再写成严格播放边界或 Ras 样本范围。
109. 2026-05-29 继续清理手写文档中的样本口径残留：`docs/animation-system.md` 的 `TRK.flags` nibble 汇总不再使用未标注的 Ras 5,193 条 track 计数，改为按 `base byte = flags & 0xFF` 的 JP/EN full-survey 计数（low nibble `0x3` 为 94,611/95,558；high nibble `0x1/0x2/0x3/0x4` 为 69,084/69,928、2,124/2,125、7,138/7,200、16,265/16,305）。`docs/sbscene-format.md` 的记录块 marker 表明确 `SLIC` 无 `RecordStartFirst/FC 00`，首条记录直接从业务字段开始。`docs/svo-resources.md` 的 `CREF.textureListIndex` 表述从 Ras-only `0` 收紧为 full-survey 分布：EN 19,734 条均为 `0`，JP 为 `0` x19,595，另有 2 条 raw `0xFFFF`/`65535` 特例。以上均为文档口径修正，未新增运行时语义。
110. 2026-05-29 继续复扫样本口径措辞：`docs/svo-resources.md` 的 `stevia::Image` 段不再只写 Ras 中 width/height 与 DDS、`_dataSize` 与 AVTS 匹配，而是明确 JP/EN full survey 各 281 条 resource record 均匹配；Ras 只保留为最早样本锚点。`docs/sbscene-format.md` 的 `NODE.0x30` 三角色 bit 表也把 bit 11 的“全覆盖”改为三角色样本限定，并指向后文 full-survey 例外，避免误读为全局必有 bit。

111. 2026-05-29 继续围绕 `CAM.0x14` 的 flags-like/unknown 口径补强 raw 证据：新增只读 `Camera*` survey 聚合并重跑 JP/EN full survey。当前 `CAM` 每场景 1 个块，字段顺序固定为 `0x03,0x12,0x13,0x14,0x15,0x16`；`0x03=default`、`0x14=8191/0x1FFF`、`0x15=10`、`0x16=100000` 全覆盖，`0x14` bit `0..12` 全置位。`docs/sbscene-format.md` 已写成 full-survey raw 结构事实，但仍不把 `0x14` 套用 packed-angle 公式，也不命名运行时 camera flags。

112. 2026-05-29 继续补齐 `LAYR` 的 raw 结构事实：新增只读 `Layer*` survey 聚合并重跑 JP/EN full survey。JP 为 314 个 `LAYR` 块且每场景 1 个；EN 为 322 个块，其中 `MM_UI_CommonWindow.sbscene` 与 `MM_UI_NameEntry.sbscene` 各含 3 个，其余 316 个场景各 1 个。字段顺序固定为 `0x03 String, 0x20 UInt32, 0x21 UInt16, 0x22 UInt16`；`ParamHigh=4`、parsed field count 4、trailing bytes 0。`0x20` 只见 `0/0x100`，JP 为 94/220，EN 为 97/225，bit 只有 8；`0x21` 按 scene 求和等于 `NODE` 总数，JP/EN delta 均为 0；`0x22` 为 `1..32`，`ParamLow - 0x22` 为 1 覆盖 JP 313/314、EN 321/322，唯一 `2` 例外为 `MM_UI_Entry__MM_UI_Select_EntryName_ALL.sbscene`。文档已同步为 raw/count-like 关系，`0x20/0x21/0x22` 的运行时用途仍未知。

113. 2026-05-29 继续补齐 `SCN` 的 raw 结构事实：半落地的 `BuildScnSurvey` 接线已补完整，新增 `ScnNameCounts`、`ScnField04RawHexCounts`、`ScnField10/11/40/41Counts`、`ScnField10Field11Counts`、`ScnField40Field41Counts`、`ScnParamLowLayerCountDeltaCounts`、`ScnParamLowField10DeltaCounts` 与 `ScnField10LayerCountDeltaCounts`。重新生成 JP/EN full survey 后，`SCN` 每场景 1 块，字段顺序固定；`ParamLow`、`0x10` 与同场景 `LAYR` 块数全量一致，`0x11=0`、`0x40=1080` 全覆盖，`0x41` 只见 `1080/450`。文档已同步为 raw/count-like/size-like 关系，运行时语义仍 unknown。

114. 2026-05-29 继续收紧 `TEXT.0x7A` 的编码证据：`SbSceneTextRecord` 新增 `Field7AShiftJis`，JSON camelCase 输出为 `field7AShiftJis`，Markdown 与 inspect 同步展示 Shift-JIS preview；CLI full survey 新增 `TextField7AShiftJisStringCounts`。重新生成 JP/EN full survey 后，strict decode 仍为 JP `validShiftJis=105/105`、EN `validShiftJis=131/131`，且 decoded value 分布可复算。重新执行 `dotnet build .\SbScene.sln --no-restore` 为 0 warning / 0 error，`dotnet test .\SbScene.sln --no-restore --no-build` 为 22/22 通过。该改动只把可读 CP932/Shift-JIS 解码值作为只读证据暴露，不确认 `TEXT.0x7A` 的最终运行时文本编码、布局或渲染语义。

115. 2026-05-29 补齐 `TEX.0x62` 的只读 raw 证据：`SbSceneTextureAtlas` 新增 `Offset`、`Field62`、`Field62Bits`，JSON/Markdown/inspect 同步输出；CLI full survey 新增 `TextureAtlasField62Counts`、`TextureAtlasField62BitCounts`、`TextureAtlasField62CropCountCounts`、`TextureAtlasField62SizeCounts`。重新生成 JP/EN full survey 后，atlas 为 2,906/2,922，`0x62` 只见 `0x110` 与 `0x0`，分布为 JP `0x110=2625, 0x0=281`，EN `0x110=2638, 0x0=284`；bit 只有 4/8，计数为 2,625/2,638；所有 atlas 的 declared/parsed crop count 仍全量匹配。新增单元测试覆盖 `TEX` state word、bit 派生、`CROP` 归属和 JSON 输出。重新执行 `dotnet build .\SbScene.sln --no-restore` 为 0 warning / 0 error，`dotnet test .\SbScene.sln --no-restore --no-build` 为 23/23 通过。该改动只确认 `TEX.0x62` raw packed state 分布，不命名 texture wrap/filter/layout 语义。

116. 2026-05-29 将已接入的 `ProjectField*` survey 聚合重新落盘到正式 JP/EN full survey：`out/survey-surfboard-all.json` 现覆盖 314 scenes / 47 svos，`out/survey-surfboard-en-all.json` 覆盖 318 scenes / 47 svos。顶层 aggregate 中 `ProjectField55Counts` 为 JP/EN `0=314/318`，`ProjectField56TrackLastRelationCounts` 为 JP `less=25, equals=23, greater=247, missingTrackLast=19`、EN `less=23, equals=20, greater=255, missingTrackLast=20`，`ProjectFieldSequenceCounts` 固定为 `0x0000:0x0006>0x0001:0x0006>0x0005:0x0006>0x0055:0x0008>0x0056:0x0008`。`docs/sbscene-format.md` 已补充这些 `ProjectField*` 字段名作为复算入口；语义仍停留在 scene/project 级 frame end/duration raw 候选。

117. 2026-05-29 继续清理 `docs/svo-resources.md` 的三样本/当前口径残留：开头三角色 SVO 表改为“最早用于交叉验证”的 checkpoint；`TEX.0x62` 段补入 full-survey 分布 JP `0x110:2625, 0x0:281`、EN `0x110:2638, 0x0:284`；`CIMG.0x45` 的三样本范围检查改为“三角色样本阶段”限定。该改动只收紧文档措辞，不新增解析行为或运行时语义。

118. 2026-05-29 补齐 shared packed state 的统一 full-survey 聚合：`SceneSurveyRow` / aggregate 现输出 `SharedPackedStateOwnerCounts`、`OwnerRawCounts`、`OwnerBitCounts`、`OwnerLowNibbleCounts`、`OwnerMaskF0Counts`、`OwnerMaskF00Counts` 与 `OwnerUpperMaskCounts`，覆盖 `CIMG.0x48`、`CNUM.0x48`、`CSLI.0x80`、`LAYR.0x20`、`SLIC.0x83`、`TEX.0x62`。用真实 `surfboard` / `surfboard_EN` 根目录重新生成正式 survey 后，owner 总数为 JP/EN `7063/7228`、`217/217`、`26/26`、`314/322`、`108/108`、`2906/2922`；`CIMG.0x48` raw distinct 为 78/82，top raw 为 `0x408000=3331/3409`、`0x408001=1208/1221`；`CNUM.0x48=0x8000` 为 217/217；`CSLI.0x80` 为 `0x8000:19, 0x8001:6, 0x8002:1`；`SLIC.0x83` 为 `0x3:61, 0x1:23, 0x2:14, 0x0:10`。文档已同步为复算入口与跨 owner 对比证据，不提升任何 bit/enum 的业务语义。

119. 2026-05-29 补齐 AVTS header unknown word 的 relation / payload location full-survey 聚合：`SvoSurveyRow` / aggregate 现输出 `HeaderUnknownWordRelationCounts`、`HeaderUnknownWordOffsetRelationCounts`、`HeaderUnknownWordPayloadLocationCounts` 与 `HeaderUnknownWordOffsetPayloadLocationCounts`。重新生成 JP/EN full survey 后，SVO 仍为 47/47 与 47/47 全解析，AVTS directory reserved 非零为 0，YABX unparsed bytes 为 0。`0x64` 在 JP/EN 分别为 `pointer-or-residue=39/37`、`out-of-file-or-unknown=8/10`；`0x68` 为 `out-of-file-or-unknown=37/32`、`pointer-or-residue=10/15`；`0x6C` 两组均为 `inside-payload:DDS=46`、`inside-payload:YABX=1`。`0x6C` 的 payload entry index 分布为 JP `DDS#1=35, DDS#2=6, DDS#3=3, DDS#4=2, YABX#0=1`，EN `DDS#1=34, DDS#2=6, DDS#3=4, DDS#4=2, YABX#0=1`；两组 `relative/end-minus` 组合各 44 种，不能命名为固定 payload 内偏移或具体 DDS 子结构。

## 本轮并行解析结果

| 子代理 | 结论 |
| --- | --- |
| Fermat | `TRK.flags` base byte low nibble 固定 `0x3`，base byte high nibble 对应 `KEY.0x5B` 存储类型；type 18 在 validator 中对应 primary 组 image/crop 变体索引，type 19 对应 secondary 组范围检查；type 21/22/23 和 25/26/27/28 分别为材质色与 illumination 色通道候选；type 24 为 alpha/opacity；`KEY.0x5D/0x5E` 在三样本阶段已证明不能全局合并，后续 full survey 又确认 mismatch 覆盖 9 个场景和多个 track type。 |
| Popper | `CIMG.0x44` 不是 secondary flag，而是 primary/secondary CREF 组记录数；Ras 共 350 条 CREF 记录，其中 primary 338、secondary 12，Chiffon 没有 secondary CREF，Otohime 有 1 个 secondary CREF；`CIMG.0x45` 已确认是 primary/secondary 组内引用 index，且与 type 18/19 最早 key 经常一致但非全覆盖，高层默认/当前/选中命名仍未确认；`CIMG.0x48` 已输出 full value 和 bit 分布。后续 MM_CH 9 场景 survey 发现 bit 4/5，并将 bit 15 修正为 1675/1676 个 CIMG 设置而非全局必有；再后续运行时代码复核将 `0x48` 收窄为跨资源复用的 packed state word。 |
| Nietzsche | AVTS header unknown 区约 50 个非零字节，三样本目录 reserved 区合计 14/14 项为 0；YABX header hash 候选每样本不同；YABX reference base 在 Ras/Chiffon/Otohime 均为 `0x2711`，descriptor raw 与三字节分布稳定一致，第三字节 123/123 为 0。后续 full survey 将 SVO 覆盖扩展为 47/47 与 47/47，YABX unparsed bytes 为 0，并发现 `_format=1` 覆盖 DXT5、A8R8G8B8、DXT1、A4R4G4B4、R5G6B5、R8G8B8。 |
| Lovelace | 只读调查确认 CRFD 在 JP/EN full survey 中分别覆盖 36/37 个文件、614/622 块；DATA following tag 中 CRFD 合计同为 614/622。所有已观察 CRFD 均为 `ParamLow=0 / ParamHigh=7` 且字段集合固定；`0x51` 全部落在 NODE index 范围内。`0x90|0x91` 组合均为 94 类且每组只对应一个 `0x92` 值；`0x90/0x91` 仍只能写为来源场景/资源名候选，`0x92` 和恒定字段语义仍 unknown。 |

## 历史并行子代理分工（已完成）

以下分工是本轮解析过程的历史记录，不再作为待办项。相关输出已经合并到 survey JSON、文档和上方完成状态表；未提升为 confirmed 的内容继续保留为 candidate/unknown。

| 子代理 | 范围 | 完成状态 |
| --- | --- | --- |
| Fermat | 动画系统：`TRK.flags`、track type `2/3/4/8/18/24`、`KEY.0x5C/0x5D/0x5E`。 | 字段分布、样本定位和可提升/不可提升边界已合并；`0x100`、tangent、重复 frame 规则仍为 candidate/unknown。 |
| Popper | 资源系统：`CIMG.0x44/0x45/0x48`、`CROP/CREF` compact selector、多 crop 与 secondary CREF 组。 | compact record、计数关系、索引范围和异常样本已固化；selector 已确认不是业务 kind，sentinel、运行时选择规则仍 unknown。 |
| Nietzsche | SVO/YABX：AVTS header unknown words、YABX hash 候选、字段描述符、reference id base。 | 47+47 个 SVO 的 header/YABX/schema/resource skeleton 证据已合并；hash/checksum、descriptor 精确语义仍 unknown/candidate。 |
| Lovelace | CRFD 资源块：出现范围、字段集合、DATA following tag 计数和 0x51 范围校验。 | CRFD 字段集合、出现范围、`0x51` 范围和 DATA following tag 关系已固化；运行时语义仍 unknown。 |
